# ホロドリ編成お助けツール (HolodoriOptimizer)

『hololive Dreams』(ホロドリ)のパーティ編成を最適化する**非公式ファンツール**。サイト上の表示名は「ホロドリ編成お助けツール」。条件(手持ち・除外・固定・曲)を与えると、ユニットスコア(試算値)が最大になるリーダー+メンバー 5 人の組み合わせを探索し、上位候補を提示します。

> **免責**: 本ツールはファンによる非公式ツールであり、カバー株式会社・株式会社 QualiArts とは一切関係ありません。ゲーム内の名称等の権利はすべて各権利者に帰属します。スコアは試算値であり、実際のゲーム内の値と異なる場合があります（総合力とユニット編成画面の「ユニットスコア」を再現するモデルで、青ホロメンボードとスコアサポートの反映には仮説を含みます。実際に曲をプレイしたときのライブスコアは計算していません）。権利者からの要請があれば速やかに公開を停止します。

## 機能

- 全★5 カード・楽曲・スキルのデータベース(テキストのみ・手入力)
- ユニット編成の最適化: リーダー・メンバーは指定してもおまかせでも可。ユニットスコア(試算値)で順位づけし、上位 10 件を 1 件ずつ左右に送って提示(タップで内訳。内訳はスコア → リーダー → メンバー 5 人 → メンバー別の表の順で、総合力とスコアボーナスの内訳はスコアの隣の「詳細」で開く。リーダー・メンバーのタイルをタップするとそのカードの詳細が開く。下端のボタンで前後の順位へ送れる)
- アカウントの登録(Step 0): ホロメン(ホロメンボード)・メンバー(持っているカードと開花段階)・ユニット(お気に入り編成)をここで登録する(ブラウザ内にのみ保存)
- ダークモード / ライトモード: ヘッダ右上のメニュー（真ん中の区分）で切り替える（既定はライト。選んだモードはブラウザに保存し、再読み込みしても保つ）
- お気に入りユニット: 結果の 1 件の右上の星から、その編成を 1〜10 の番号へ登録する(登録済みの番号は上書きの確認つき。星をもう一度押すと解除)。結果詳細からも登録・解除でき、Step 0 の「ユニット」で登録した編成を番号順に送って見られる(そこからも解除できる)。保存するのはカードの組み合わせだけで、スコアは開いたときの登録状態(開花・ボード・メモリー・強化ボーナス)で計算し直す。ゲームのユニット編成画面と同じく曲は反映しない(曲の反映は「さがす」の結果側だけ)
- 発動頻度のおすすめ: 結果の詳細・お気に入りユニットの詳細の下の「発動頻度のおすすめ」から、青ホロメンボードの「アクティブスキル発動頻度」のマスを**誰に何個開けるとよいか**を比べる(その編成のメンバー 5 人について、いま登録しているボードから実際に到達できる状態を全組合せで評価する。頻度マスまでの経路も解放されるので「追加で解放するマス」の数はその経路を含む。いま開けている頻度マスを外す案も比べるので、現在より少ない頻度がおすすめになることもある)。指標は「期待値重視」(各アクティブの発動確率を考慮し、同時発動時は最も高いスコア UP だけが有効という前提の期待値を時間平均した値)と「理論最大重視」(発動抽選がすべて成功した前提での最大スコア UP の時間平均)の 2 つで、画面上部のトグルで切り替える(既定は期待値重視)。期待カバレッジ・最大空白は「見込み」の補助表示。評価区間はこの画面の曲ピッカーで選んだ曲の演奏時間(既定は編成をさがしたときに指定していた曲。指定しないときは全曲の中央値)。**実際のライブスコアではありません**(譜面・コンボ・判定・スペシャルスキルの発動位置・スコアサポートを含まない試算値で、発動頻度 ÷ (1 + f/100)・発動率 × (1 + r/100) の反映は仮説。[ADR-007](docs/adr/007-live-frequency-optimizer.md))
- 探索対象の絞り込み: 一部メンバーを固定する(メンバーの 5 枠はどれをタップしても同じピッカーが開き、タップで複数のカードを固定・解除できる。何人目に固定したかはカードの背景の透かし数字)。さがすステップのオプション(既定は畳んである)で、所持カードから探すか(既定 ON)、リーダーから除外・メンバーから除外(それぞれ候補から外すカード。ボタン内に枚数。指定したリーダー・固定したメンバーには効かない)、ボード状況・開花状況を考慮するか、衣装スキル・パッシブが発動する編成だけに絞るかを選べる(設定はブラウザ内にのみ保存。除外は保存しない)
- 総合力の試算: ゲームのユニット編成画面の「総合力」と同じ 6 項目(メンバーパラメータ・衣装スキル・ホロメンボード効果・パッシブスキル・メモリー効果・メンバー強化ボーナス)を、ボード前の本体値を基準に別々に求めて加算する(2026-09-08 の実機観測に基づくモデル。結果の詳細に内訳とメンバー別の値を表示)。イベントメモリーの「ユニットパラメータ +X%」とメンバー強化ボーナス +X% は Step 0 の 2 つのボタン（名前と現在値が出る）から、数字と小数点だけのテンキーで入力する（スマホのキーボードは出ない。0〜50% で、イベントメモリーは小数第 1 位・メンバー強化ボーナスは小数第 2 位までとし、それを超える入力は受け付けない。ボタンの値もその桁まで常に出す）。ゲーム画面の「スコアボーナス」4 項目(アクティブスキル・ホロメンボード効果・パッシブスキル・スペシャルスキル)と「ユニットスコア」(総合力 × (1 + スコアボーナス) × 係数)は、曲を選ばない 200 秒の仮想タイムラインで発動確率を重みづけするモデルで試算し、結果の値と順位づけはこのユニットスコア(曲を選んでいれば黄の楽曲スコアボーナスをホロメンボード効果欄に含む)× (1 + イベントスコアボーナス)で行う(各欄は 0.1% 単位の切り上げで整数化する。実機 20 ケースでスコアボーナス合計 −0.8〜+2.3 ポイント、ユニットスコア −0.4%〜+1.0%。項目ごとは アクティブ **20 ケース全一致** / SP −0.4〜+0.6 / ボード −1.5〜+2.7 / パッシブ −1.6〜+0.8 ポイントで、青ボードと赤・パッシブのスコアサポートの欄への配賦が未解明の部分。探索は上限値で候補を絞ってから正確に評価する近似で、真の上位が落ちないことは小規模の厳密探索と突き合わせて検証している)
- 開花(凸)の反映: 持っているカードはカードごとに開花段階(0〜5)を登録でき(変更は Step 0 のみ。メンバーピッカーには段階のアイコンだけ出る)、「開花状況を考慮する」が ON のとき段階に応じた試算に反映される(考慮しないとき — 全カードからさがすときも — は全カードを開花最大として試算する。2凸のパラメータ +10% は実測値。スキルの段階別の実数値が未確認の間は仮定倍率による概算)
- ホロメンボードの反映: ホロメンごとに、ゲーム内と同じ配置のボードで解放したマスを登録できる(左右の配置はホロメンごとの固定データで自動、経路のまとめて解放、すべて解放/解除、マスの効果を読む「説明」モード)。赤・青・黄・緑の 4 色。ボードはカードでなくホロメンの状態として持ち、持っているカードで「ボード状況を考慮する」が ON のときに効く(考慮しないとき — 全カードからさがすときも — は全ホロメンの 4 色すべてを全解放した状態として試算する。将来に向けた探索は育てきった前提で比べる — 2026-09-08 ユーザー判断)。青はそのホロメンのカードのパラメータと表示スコアボーナスのアクティブ発動の試算に、緑は登録した全ホロメン分の合計が全カードに(全員向けの P/T/S UP と、所属向けの全パラメータ UP — 1 枚あたり +900 が上限)効く。黄(本人のソロ / 本人を含むユニット / 全体楽曲のスコアボーナスとホロワークの報酬)は、曲を指定したときにその曲に乗る全ホロメン分の合計(上限 10.0%)がスコアボーナスのホロメンボード効果欄に入る(2026-09-11 の実機観測に基づく。ホロワークの報酬は表示のみ)。赤(リーダー用)はそのホロメンをリーダーにした編成のメンバー 5 人に効き、全員の P/T/S の固定値と割合を総合力に足し、「全員のスコアサポート効果 +X%」は表示スコアボーナスの合計を 0.88 × X ポイント増やす(2026-09-11 の実機観測で確認した換算。ゲームではホロメンボード効果欄とパッシブ欄の両方が動くが配賦の式は未解明で、ツールは増分をホロメンボード効果欄に入れる近似)。歌唱者条件(「このホロメンが楽曲歌唱者に含まれる時」の P/T/S +10% とスコアサポート)は曲を指定し、リーダーのホロメンがその曲の歌唱者に含まれるときに効く(P/T/S は総合力のホロメンボード効果へ — 2026-09-11 の実機観測に基づく)。ライフ・ホロメンスキル・ライブ報酬はスコアの試算には使わず表示のみ(63 マスは左 / 上 / 右の 3 エリアに分けて表示し、枝が画面の外へ続く位置の出口をタップして移動)。いずれもマスの表記値の合計で試算する。**コネクト効果**は、ボード画面の人物アイコン(中心・赤・青・黄の 4 か所)をタップしてカードを置くと、そのカードのコネクト効果(「範囲内のホロメンボード効果を X% UP」)が範囲内の解放済みマスの効果を 元値 × (1 + X/100) にする暫定モデルで試算する(範囲と X はゲーム画面に数値が出ないため外部の公開情報由来の暫定値で、実機確認済みではない。どのカードがどの効果を持つかは確認できたカードだけデータに入れており、データのないカードは置いても増幅されない。ボード状況を考慮しないときは置くカードを決めず増幅なし)
- 曲を指定した最適化（曲名・ホロメン名の検索、所属・オリジナル/カバーで絞り込み、曲長・レベルで並べ替え）。曲を指定すると黄ホロメンボードの楽曲スコアボーナスがスコアボーナスのホロメンボード効果に加わり（2026-09-11 の実機観測に基づく。曲によって候補の順位が変わることがある）、イベントスコアボーナスが順位づけの値に掛かる。**曲の長さ・譜面は試算に使っていない**（実際のライブ中のスコアは未実装 — 下の「将来」）
- データの取り込み: ゲームのスクリーンショットを**自分の端末のローカル LLM** に読ませて構造化データ（JSON）を作り、それをサイドメニューの「データの取り込み」に貼り付けて持っているメンバー（★5 のカードと開花段階）を一括登録する。画面にコピーできるプロンプト（「次のスキルを実行してください」+ [.claude/skills/structure-import](.claude/skills/structure-import) の URL）が載っているので、それをスクリーンショットと一緒に手元の AI へ渡すだけでよい（読み取りの手順・出力の形はスキル側に書いてある）。スクリーンショットも貼り付けた内容もどこにも送信されない（貼ったデータはそのブラウザの localStorage に入るだけ）。取り込みは「貼る（「ペースト」ボタン 1 つでキーボードを出さずに済む）→ はい / いいえ の質問 → 取り込むカードの確認 → 取り込む」の流れ。読み替えや開花段階の未読取のように人が決めることは中央のダイアログで 1 問ずつ答え（全部答えるまで先に進まない）、人が決められないもの（見つからないカード名・ホロメン名の食い違いなど）は「取り込みエラー n 件」として件数だけ出る。取り込むとシートは閉じてメイン画面に戻り、データに無いカードの登録が消えることはない
- ヘッダ右上のメニュー(ハンバーガー → 右のサイドバー): 上から データの取り込み、カード一覧(★5 全カードのパラメータ・スキル・開花段階ごとの強化内容の詳細)、曲一覧(歌唱・種別・演奏時間・難易度別の Lv とノーツ数)、仮想ガチャ / ダークモード / ライトモードの切り替え、絶対おかゆんモードの ON / OFF / ソースコード(GitHub)、管理用画面(折り畳み。「カラー確認」= ヘッダ・フッタ・背景の色をモードごとに試す配色の調整、変更はブラウザ内にのみ保存。「ホロメンボード」= デバッグ用のホロメンボード（複数ホロメン × 4 色）と構造化データ (JSON) を同じ画面で双方向に見る。ボードのマスを触ると JSON が書き換わり、JSON を貼る・直すとボードがその状態になり、2 回目以降は前回との差分が出る。デバッグ用の状態は閉じても残るが、登録しているボードは書き換えない)。データにない項目(コネクト効果の内容・作詞作曲編曲など)は「未確認」と表示
- 仮想ガチャシミュレーション(ピックアップガチャ、仮想ダイヤと課金相当額の表示。実際の課金・排出とは無関係の遊び機能で、状態はブラウザ内にのみ保存)
- (準備中)イベント対応: 現在・過去の全イベント(開催期間・対象カード・課題曲)をデータとして持ち、編成のイベント Pt / バッジ獲得ボーナス(メンバー + ホロメン + 開花。リーダー枠は対象外)を計算できる。獲得ボーナスが最大の編成をさがす画面は設計を見直し中
- (将来)実ライブスコアの最適化: 実際に曲をプレイしたときのスコア(譜面・コンボ・SP の発動位置・アクティブの重なりなど)の計算は**未実装**で(上の「発動頻度のおすすめ」はアクティブスキルの発動タイミングだけを見る別モデルで、ライブスコアの再現ではありません)、現在の順位づけはユニット編成画面の「ユニットスコア」の再現値で行っている。ライブスコアの式が実機で解明できたら、表示ユニットスコアとは別のエンジンとして追加する([ADR-006](docs/adr/006-actual-live-score-separate-engine.md))
- (将来)コネクト効果のカード別データ(どのカードがどの効果を持つか)の実機確認と、ボード状況を考慮しないときの最適なコネクト配置の探索

## 開発状況

https://hoshock.github.io/HolodoriOptimizer/ で公開中。計画は [docs/ai/tmp/plan.md](docs/ai/tmp/plan.md)、設計判断は [docs/adr/](docs/adr/index.md)、ドキュメント全体の索引は [docs/index.md](docs/index.md) を参照。

## 技術スタック

Vue 3 + TypeScript / Vite+ (`vp`) / pnpm。GitHub Actions でビルドし GitHub Pages で配信する静的サイト(サーバなし・計測なし)。

## ライセンス

本リポジトリのコードは [MIT License](LICENSE) です。ただし、リポジトリ内のゲームデータ(カード名・スキル効果等のゲーム由来のテキスト情報)の権利は各権利者に帰属し、MIT License の対象外です。
