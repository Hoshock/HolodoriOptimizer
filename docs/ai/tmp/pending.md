# 保留事項

**現在も未解決のものだけ**を置く。解決履歴はGit履歴へ、観測は `docs/human/repro/`、証拠の扱いは `docs/human/evidence-policy.md` を正典とする。

## 最優先: データ・観測の再監査

1. **トップレベルカードレコードの歴史的出典分類**
   - `cards.json` のトップレベルは最大開花側レコードとして扱う。
   - 既存 `bloomVariants` は `src/data/bloomEvidence.ts` で全件 `observed-text / observed-values-reconstructed-text` 等に分類済みで、未分類variantがあれば `bloomEvidence.test.ts` が失敗する。
   - 最大値から `÷1.1` した未確認スキル値は `estimated-from-max`、2凸+10%から逆算したpre-2凸statsは `derived-from-max-confirmed-ratio` として `cardAtBloomWithProvenance()` が区別する。
   - 残る課題は、トップレベル各カードレコード自体が「初期公開データ転記 / 実機入力 / 後日訂正」のどれに由来するかを、Git履歴や元資料で追える範囲まで分類すること。追跡不能なものを推測で実機確認済みに昇格しない。
   - 実機訂正は `cardCorrections.ts` とテストで固定し、可能な範囲で取り込み元 `cards.json` も同期する。
2. **2026-09-12表示スコアGoldenの入力条件再確認**
   - 数値はモデル都合で変更しない。
   - 編成順、Lv、開花、青ボード状態などが会話由来のみのケースは `reported` とし、必要な実験に使う前に再確認する。
   - unit score式による算術cross-checkは入力条件の再確認とは別。
   - 水着フワワの青: 2026-09-09 の実機報告は発動率 +45% だが、2026-09-12 の構造化データにはフワワの青マスがない。`displayScore.test.ts` の `BLUE_AT_FUWAWA_OBSERVATION`（45 / 0）と `displayScoreExperimental.test.ts` / `displayScoreAttributionExperimental.test.ts`（0 / 0）で食い違うので、カード画面の表示値を再確認する。水着みこの青も 36（09-09）と 42（構造化データ）で同種の衝突。総量モデルの感度（0/0: RMSE 0.09、45/0: 0.20）は裁定に使わない。

## 表示ユニットスコア

3. **赤スコアサポート — 総増分は強い推定、残る主問題はカテゴリ配賦**
   - 旧 `X × 基準候補秒 / 200` は反証済み（production には既知の近似として残る）。
   - 総増分 `ΔRed(X) = X/100 × E_blue`（乗算型の青入りアクティブ期待値）が実機13対照で RMSE ≈ 0.09 pt・最大 ≈ 0.13 pt（`src/engine/displayScoreExperimental.ts`、production 未採用）。加算型は ≈ 0.85 pt、旧式は ≈ 1.0 pt。
   - **次の逆解析対象はこの総増分が Costume / Board / Passive の各サーバーカテゴリへどう配賦されるか**（赤+24で Costume +0.5 / Board +20.4 / Passive 0、+0.4 / +18.6 / +0.1、−0.1 / +21.0 / 0 など）。配賦が決まるまで production の表示計算へ総増分を繋がない。
   - 2026-09-12: 既存評価器上の逐次 marginal attribution（S / L / P の 6 順序 × 供給側確率 2 × 量子化 2）は全組が反証（`src/engine/displayScoreAttributionExperimental.ts`）。L が S の後なら衣装 ΔC ≈ +5、前ならボード ΔB ≈ +5 過大。衣装欄は「一様 ×1.25」の下限 `0.25 × E_base`（17.7〜19.9）を全 9 編成で下回る（13.4〜17.4）ので、衣装の内部式が先に必要。
   - 衣装なし対照でもパッシブ増分（+10 で +0.4 / +0.5 / +0.3、+24 で +0.2）を既存の支援行列は再現しない（+0.2 / +0.1 / +0.1、+0.5）。
   - exploratory 行（恒常みこ Lv 約 20）は総量モデルで約 +1.0 の未説明差。Lv・スキル Lv の再確認まで fit に入れない。
   - 次の 1 実験: みこ1 / 水着ミオ1 / 水着フワワ0 / 水着おかゆ5 / 恒常マリン1 で FUWAMOCO のまま赤 1 マス（R-053 +3 等）だけ開閉し、衣装 / ボード / パッシブを再読する。
   - 残差 ≤ 0.2 pt の由来（量子化・青の実効値の再確認）と、青の乗算型が一般仕様かどうかは別課題。
   - 水着フブキとの相関は観測として残すが、特定パッシブを原因とする仮説は撤回済み。次の実験は事前にカードID・開花・role・provenanceを監査し、変更要因を最小化する。
4. **5カテゴリの内部式**
   - Costume算出式（一般式未解明。現行は静的倍率の既知の近似。一様倍率は 3 で棄却済みなので、静的倍率は表示上限としてだけ扱う）。
   - 赤増分の Costume / Board / Passive の各カテゴリ式（3 を参照）。
   - Passiveとの相互作用（赤・衣装のスコアサポートがパッシブ欄も動かす）。
   - カテゴリ単位の permil 量子化。
   - Passiveスコアサポートの対象・合成位置。
   - 青の発動率UP / 発動頻度UPのBoard換算。
   - Costume / Board / Passiveの厳密な0.1%量子化。
   - （2026-09-12 に実装側は5カテゴリ構造と黄の5カテゴリ式へ更新済み。Costume欄の値は静的倍率の既知の近似のまま）
5. **SP残差**
   - 発動率UPを含むケースなどのずれを説明する。

## パラメータ・ボード

6. **赤割合効果の丸め単位**
   - 現実装は一部で±1〜2程度ずれる。Goldenを丸め仮説へ合わせない。
7. **コネクト効果**
   - 範囲、重複、割合・‰丸め、黄/赤/緑への適用、5凸時の扱いを実機確認する。
8. **開花途中の実数値**
   - 未確認カードのvariantを追加し、最大値からの推定依存を減らす。

## 実ライブ・イベント

9. **イベントスコアボーナスの適用位置**
10. **実ライブスコアエンジン**

## データ・公開

11. **権利ガイドライン原文の人間確認**
12. **初期データの抜き取り実機照合**
13. **仮想ガチャの未確認★3/★4・ダイヤパック**
