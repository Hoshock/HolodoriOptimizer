# 保留事項

**現在も未解決のものだけ**を置く。解決履歴はGit履歴へ、観測は `docs/human/repro/`、証拠の扱いは `docs/human/evidence-policy.md` を正典とする。

## 最優先: データ・観測の再監査

1. **トップレベルカードレコードの歴史的出典分類**
   - `cards.json` のトップレベルは最大開花側レコードとして扱う。
   - 既存 `bloomVariants` は `src/data/bloomEvidence.ts` で全件 `observed-text / observed-values-reconstructed-text` 等に分類済みで、未分類variantがあれば `bloomEvidence.test.ts` が失敗する。
   - 最大値から `÷1.1` した未確認スキル値は `estimated-from-max`、2凸+10%から逆算したpre-2凸statsは `derived-from-max-confirmed-ratio`、抽出マスター（外部解析）から転記した途中値は `extracted-master-variant` として `cardAtBloomWithProvenance()` が区別する。
   - 残る課題は、トップレベル各カードレコード自体が「初期公開データ転記 / 実機入力 / 後日訂正」のどれに由来するかを、Git履歴や元資料で追える範囲まで分類すること。追跡不能なものを推測で実機確認済みに昇格しない。
   - 実機訂正は `cardCorrections.ts` とテストで固定し、可能な範囲で取り込み元 `cards.json` も同期する。
2. **2026-09-12表示スコアGoldenの入力条件再確認**
   - 数値はモデル都合で変更しない。
   - 編成順、Lv、開花、青ボード状態などが会話由来のみのケースは `reported` とし、必要な実験に使う前に再確認する。
   - unit score式による算術cross-checkは入力条件の再確認とは別。
   - **2026-09-13 の SP 観測（[repro/display-score-20260913-sp.md](../../human/repro/display-score-20260913-sp.md)）の衣装欄が未共有。** 報告された 4 欄の合計がちょうど 合計 168.2 になるので「衣装欄 0」と読めるが、リーダーの 水着フワワ 0凸 の衣装スキル「ピュアタイプ 2 人以上で全員のスコアサポート 25%」はこの 5 人（ピュアは 水着みこ・水着ころね の 2 人）で成立するはずで食い違う。装備していた衣装と衣装欄の実機値が要る。SP 欄の裁定には影響しない（SP は衣装・赤・黄・リーダーで動かない）。
   - **水着フワワ / 水着みこ の青が 2026-09-12 時点でいくつだったかが未確定。** フワワは 2026-09-09 の実機報告 45 / 0 と 09-12 の構造化データ（青マスなし = 0 / 0）で食い違い、`displayScore.test.ts` の `BLUE_AT_FUWAWA_OBSERVATION`（45 / 0）と experimental 側（0 / 0）で扱いが割れたまま。みこも 36（09-09）と 42（構造化データ）で同種の衝突。総量モデルの感度（0/0: RMSE 0.09、45/0: 0.20）は裁定に使わない。2026-09-13 の export の 24 / 0 は**ボード振り直し後の別時点**（ユーザー確認済み）なので 09-12 の裁定に使えない。決着させるにはその時点のボードを再現できる情報が要るが、振り直しの履歴は残っていない。

## 表示ユニットスコア

3. **`W_blue` の発動頻度側の正規化（表示 5 欄で最後に残った未確定）**
   - 2026-09-13 に「青を実効スコアサポート % に直す」形へ替えた（`displayScore.ts` の `blueSupportPercentOf`、正典は `docs/human/display-score.md`「9. `W_blue` の決定式」、観測は [repro/display-score-20260913-blue-weight.md](../../human/repro/display-score-20260913-blue-weight.md)）。**確定ではなく現時点の最有力 production model**。ここには**まだ決まっていないこと**だけを置く。
   - **発動頻度側の分母が決まらない。** 自由係数なしの自然量 7 つで総当たりすると アクティブ欄 raw が最良だが、**表示アクティブ欄（0.1% 切り上げ）とは逸脱が 0.0001 も違わず分離できない**。次点の `H_C` も 2.5 倍悪いだけで、桁違いには棄却できていない。発動率側の `Σ 単独寄与` は次点の 20 倍以上良いので、そちらは決定的。
   - **なぜ 2 つの分母が違うのかが導けていない。** 比はこの編成で 1.259 で、発動頻度 1pt が発動率 1pt の約 1.26 倍効くことになる。
   - **20 状態中 8 状態が切り上げの要求区間の外**（S1 / S2 / S3 / S3' / F16 / RmF / K1 / K4）。逸脱はほとんど 0.0001〜0.0006 で、最大は K1 の 0.0026（表示でボード欄 0.2 pt）。モデルが要求区間の**下側**に外れる向きで揃っているので、小さい系統的な偏りが残っている。
   - K1 の 水着フワワ の青は 09-09 報告（45 / 0）と 09-12 の構造化データ（0 / 0）で割れたままなので（項目 2）、当てはまりを provenance の裁定に使わない。
   - **要求区間は量子化規則の仮定で変わる。** 3 欄の規則が未確定（項目 4）なので、切り上げ `(d − 0.1, d]` と 四捨五入 `[d − 0.05, d + 0.05)` を分けて評価する。対称な ±0.05 だけで評価すると四捨五入を仮定したことになる。
   - exploratory 行（恒常みこ Lv 約 20）は総量モデルで約 +1.0 の未説明差。Lv・スキル Lv の再確認まで fit に入れない（恒常みこ 0凸の 4 スキルは `estimated-from-max` のまま）。

4. **Costume / Board / Passive の 0.1% 量子化規則**
   - 切り上げ / 四捨五入が決着しない。コーパス 153 列で切り上げ 59 列・四捨五入 57 列（`displayScoreQuantization.test.ts`）。切り上げがわずかに先行した（2026-09-13 の `W_blue` 更新前は 46 対 44）。
   - 最小の矛盾集合は {K5, K6}: K5 は切り上げだと必ず 0.1 高く、K6 は四捨五入・切り捨てだと必ず 0.1 低い。**raw の残差が 0.1% を切るまで判定できない**（2026-09-13 の `W_blue` 更新で最大 0.8 → 0.4 pt まで下がったが、まだ届かない）。
   - **項目 3 と循環している**: `W_blue` の候補比較は表示値から raw の区間を作るので量子化規則の仮定が要り、量子化規則の判定は raw の残差が小さくなるのを待つ。どちらも「切り上げ / 四捨五入を別々に評価して、結論が仮定に依存しないかを見る」形で扱う。
   - production は「5 欄で規則を 2 つ持たない」ほうを採り、アクティブ欄・SP 欄と同じ切り上げにそろえてある。

## パラメータ・ボード

5. **赤割合効果の丸め単位**
   - 現実装は一部で±1〜2程度ずれる。Goldenを丸め仮説へ合わせない。
6. **コネクト効果**
   - 範囲、重複、割合・‰丸め、黄/赤/緑への適用、5凸時の扱いを実機確認する。
   - `liveFrequencyOptimizer.ts` がコネクト増幅を入れていない件は**既知の実装ギャップ**として `.claude/rules/engine-structure.md` が正典に持つ（直し方もそこ）。直すか残すかのユーザー判断だけがここの未解決。
7. **開花途中の実数値**
   - 未確認カードのvariantを追加し、最大値からの推定依存を減らす。抽出マスター由来の値は `extracted-master-text` として出所を残し、実機目視が取れたら `observed-text` へ昇格する。master の level 番号と凸段階の対応はカード共通ではないので、level 番号だけで未観測の凸値を確定扱いしない。
   - そら / ぼたん 0凸 の実機値と抽出マスター level 1 の不一致は、公開履歴（`LiveActiveSkillLevel.json` の最終変更は 2026-09-07 `fbbb04b...`、9/7 diff に両者の変更なし、低値は effect group 自体にある）から「生成文言だけが古い」では説明できない。9/10 以降の production サーバー側の hotfix / master 差は公開情報からは否定できないままで、そら / ぼたん専用の special rule は作らない（`docs/human/card-data-provenance.md`）。
   - 解析コーパスに残る `estimated-from-max`: 恒常そら / アキ / スバル / フレア / ぼたん 0凸の Passive / SP、恒常みこ 0凸の 4 スキル（`displayScoreCategoryCorpus.audit.test.ts` で列挙。コーパスの評価には効かない）。
   - **SP 欄の式が 2026-09-13 に確定したので、恒常 0凸 の SP 支援 % は実機の SP 欄から逆算できる。** K5 / K6 / K7 はアクティブ欄が実機と一致するのに SP 欄だけ 3.8〜4.2 高く、必要な係数は試算値の 0.89〜0.92 倍（`displayScoreSpecialColumn.test.ts`）。ただし **単一の共通係数では説明できない**（K5 と K6 の区間が重ならない）ので、`÷1.1` を別の定数へ替えて済む話ではない。逆算はカード 1 枚ずつを分離できる編成が要る。

## 実ライブ・イベント

8. **イベントスコアボーナスの適用位置**
9. **実ライブスコアエンジン**

## データ・公開

10. **権利ガイドライン原文の人間確認**
11. **初期データの抜き取り実機照合**
12. **仮想ガチャの未確認★3/★4・ダイヤパック**
