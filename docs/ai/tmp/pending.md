# 保留事項

**現在も未解決のものだけ**を置く。解決履歴はGit履歴へ、観測は `docs/human/repro/`、証拠の扱いは `docs/human/evidence-policy.md` を正典とする。

## 最優先: データ・観測の再監査

1. **カードデータ provenance の全件棚卸し**
   - `cards.json` のトップレベルは最大開花側レコードだが、初期公開データ転記・実機入力・後日訂正を区別する。
   - `bloomVariants` 全件を `observed-text / observed-values-reconstructed-text` 等に分類し、未分類を残さない。
   - 最大値から `÷1.1` したスキル値は `estimated-from-max` であり、実測扱いしない。
   - 実機訂正は `cardCorrections.ts` とテストで固定し、可能な範囲で取り込み元データも同期する。
2. **2026-09-12表示スコアGoldenの入力条件再確認**
   - 数値はモデル都合で変更しない。
   - 編成順、Lv、開花、青ボード状態などが会話由来のみのケースは `reported` とし、必要な実験に使う前に再確認する。
   - unit score式による算術cross-checkは入力条件の再確認とは別。

## 表示ユニットスコア

3. **赤スコアサポートの一般式**
   - 旧 `X × 基準候補秒 / 200` は反証済み。
   - 赤+24の報告差は現在+16.4〜+20.9pt。
   - 水着フブキとの相関は観測として残すが、特定パッシブを原因とする仮説は撤回。次の実験は事前にカードID・開花・role・provenanceを監査し、変更要因を最小化する。
4. **5カテゴリの内部式**
   - Costume算出式。
   - 赤増分の Costume / Board / Passive の各カテゴリ式。
   - Passiveスコアサポートの対象・合成位置。
   - 青の発動率UP / 発動頻度UPのBoard換算。
   - Costume / Board / Passiveの厳密な0.1%量子化。
   - 黄の5カテゴリ式へ実装を合わせる。
5. **SP残差**
   - 発動率UPを含むケースなどのずれを説明する。

## パラメータ・ボード

6. **赤割合効果の丸め単位**
   - 現実装は一部で±1〜2程度ずれる。Goldenを丸め仮説へ合わせない。
7. **コネクト効果**
   - 範囲、重複、割合・‰丸め、黄/赤/緑への適用、5凸時の扱いを実機確認する。
8. **開花途中の実数値**
   - 未確認カードのvariantを追加し、最大値からの推定依存を減らす。

## 実ライブ・イベント

9. **イベントスコアボーナスの適用位置**
10. **実ライブスコアエンジン**

## データ・公開

11. **権利ガイドライン原文の人間確認**
12. **初期データの抜き取り実機照合**
13. **仮想ガチャの未確認★3/★4・ダイヤパック**
