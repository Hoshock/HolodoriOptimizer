# 保留事項

**現在も未解決のものだけ**を置く。解決履歴はGit履歴へ、観測は `docs/human/repro/`、証拠の扱いは `docs/human/evidence-policy.md` を正典とする。

## 最優先: データ・観測の再監査

1. **トップレベルカードレコードの歴史的出典分類**
   - `cards.json` のトップレベルは最大開花側レコードとして扱う。
   - 既存 `bloomVariants` は `src/data/bloomEvidence.ts` で全件 `observed-text / observed-values-reconstructed-text` 等に分類済みで、未分類variantがあれば `bloomEvidence.test.ts` が失敗する。
   - 記録のない開花段階は `unknown`（文言は「未確認」、計算は最近傍の凸を流用 — [ADR-011](../../adr/011-bloom-text-known-or-unknown.md)）、2凸+10%から逆算したpre-2凸statsは `derived-from-max-confirmed-ratio` として `cardAtBloomWithProvenance()` が区別する。
   - 残る課題は、トップレベル各カードレコード自体が「初期公開データ転記 / 実機入力 / 後日訂正」のどれに由来するかを、Git履歴や元資料で追える範囲まで分類すること。追跡不能なものを推測で実機確認済みに昇格しない。
   - 実機訂正は `cardCorrections.ts` とテストで固定し、可能な範囲で取り込み元 `cards.json` も同期する。
2. **2026-09-12表示スコアGoldenの入力条件再確認**
   - 数値はモデル都合で変更しない。
   - 編成順、Lv、開花、青ボード状態などが会話由来のみのケースは `reported` とし、必要な実験に使う前に再確認する。
   - unit score式による算術cross-checkは入力条件の再確認とは別。
   - **2026-09-13 の SP 観測（[repro/display-score-20260913-sp.md](../../human/repro/display-score-20260913-sp.md)）の衣装欄が未共有。** 報告された 4 欄の合計がちょうど 合計 168.2 になるので「衣装欄 0」と読めるが、リーダーの 水着フワワ 0凸 の衣装スキル「ピュアタイプ 2 人以上で全員のスコアサポート 25%」はこの 5 人（ピュアは 水着みこ・水着ころね の 2 人）で成立するはずで食い違う。装備していた衣装と衣装欄の実機値が要る。SP 欄の裁定には影響しない（SP は衣装・赤・黄・リーダーで動かない）。
   - **水着フワワ / 水着みこ の青が 2026-09-12 時点でいくつだったかが未確定。** フワワは 2026-09-09 の実機報告 45 / 0 と 09-12 の構造化データ（青マスなし = 0 / 0）で食い違い、`displayScore.test.ts` の `BLUE_AT_FUWAWA_OBSERVATION`（45 / 0）と experimental 側（0 / 0）で扱いが割れたまま。みこも 36（09-09）と 42（構造化データ）で同種の衝突。総量モデルの感度（0/0: RMSE 0.09、45/0: 0.20）は裁定に使わない。2026-09-13 の export の 24 / 0 は**ボード振り直し後の別時点**（ユーザー確認済み）なので 09-12 の裁定に使えない。決着させるにはその時点のボードを再現できる情報が要るが、振り直しの履歴は残っていない。

## 表示ユニットスコア

3. **`W_blue` の発動頻度側の正規化**
   - 現在の式（`displayScore.ts` の `blueSupportPercentOf`）は**確定ではなく最有力の production model**。式・候補比較・棄却した族・49 組の頑健性確認は正典（`docs/human/display-score.md`「9. `W_blue` の決定式」）が持つので**ここで再探索しない**。観測は [repro/display-score-20260913-blue-weight.md](../../human/repro/display-score-20260913-blue-weight.md)。
   - **発動頻度側の分母が決まらない。** 自然量の中では アクティブ欄 raw が最良だが、**表示アクティブ欄（0.1% 切り上げ）と分離できない**。次点の `H_C` も桁違いには棄却できていない（発動率側の `Σ 単独寄与` は決定的）。
   - **なぜ発動率側と発動頻度側で分母が違うのかが導けていない。**
   - **20 状態中 8 状態が切り上げの要求区間の外**で、外れる向きが下側にそろっている（小さい系統的な偏りが残っている）。最大は K1 の 0.0026 = 表示でボード欄 0.2 pt。
   - K1 の 水着フワワ の青は provenance が割れたままなので（項目 2）、当てはまりを裁定に使わない。
   - **要求区間は量子化規則の仮定で変わる**ので、切り上げと四捨五入を分けて評価する（項目 4 と循環している）。対称な ±0.05 だけで評価すると四捨五入を仮定したことになる。
   - exploratory 行（恒常みこ Lv 約 20）はアクティブ欄が実機と 0.5 ずれる。総量モデルの未説明差は +0.1（fit 13 対照の最大 0.128 と同じ桁）まで縮んだが、**Lv・スキル Lv が「約 20」のままなので fit に入れない**。再読待ち。

4. **Costume / Board / Passive の量子化の位相（規則の選択より手前）**
   - **2026-09-15 に前提が 1 つ落ちた。** 「衣装欄 raw = 0.60 × アクティブ欄 raw を独立に丸める」は、**アクティブ欄 3 通り × 衣装欄 3 通りの丸めすべてで {K5, K6} を両立できない**（`displayScoreQuantization.test.ts`「{K5, K6} が落とす仮説(モデル非依存)」。判定に**モデルの raw を使わない** — 実機のアクティブ欄の表示値だけで決まる）。「raw の残差が 0.1% を切れば決着する」という見立ては取り下げた。
   - **2026-09-15 に 6 編成を追加して現行の位相が落ちた。** パッシブ支援がない 4 件（K5 / 6 / 7 / 8）で、欄ごとに独立に切り上げる規則が 4/4 外れ（すべて実機より 0.1 高い）、アクティブ欄との差として量子化する形が 4/4 当たる。アクティブ欄は 8 編成すべて実機と一致しているので入力の問題ではない（[repro/display-score-20260915-costume.md](../../human/repro/display-score-20260915-costume.md)、`displayScoreCostumeQuantization.test.ts`）。
   - **置き換え先はまだ選べない。** 候補（累積 パッシブ→衣装→ボード = コーパス 61 列 / アクティブ欄との差 = 60 列 / 現行の独立 = 59 列）はどれも**パッシブ支援がある編成**を外す。とくに 編成 2 は衣装欄 raw が 0.24 足りず、パッシブ支援があると衣装欄が `0.6 × アクティブ欄 raw` より大きくなるように見える。
   - **5 欄が揃った（2026-09-15）。合計・アクティブ欄・ボード欄・SP 欄は 3 件とも完全一致**で、食い違うのは衣装欄とパッシブ欄のあいだの 0.1〜0.2 の入れ替わりだけ。総量 `T` は合っている。
   - **編成 2 は再読でも 40.6。`衣装欄 raw = 0.60 × アクティブ欄 raw` は棄却**（`0.60 × 67.3 = 40.38` でどの丸めでも 40.4 以下。総合力 106264 と合計 156.4 から ユニットスコア 555096 が外側の式で 1 点一致するので合計は `cross-checked`）。配分は projective で `W_P = ΔP` が約分されるため、対象選抜をどう変えても衣装欄は動かない。
   - **「対象選抜で説明できる」は取り下げた。** 編成 1 の実測 P/T/S（マリン 29945 / レイネ 17202 / ロボ子 17117）はモデルの選択（マリン + レイネ）と同じ順位だった。
   - 必要な raw の移動は 編成 1 `(−0.05, +0.05)`、編成 2 `(+0.16, −0.16)`、編成 3 `0`。**符号が逆なので `W_P = k × ΔP` の定数倍では両立しない**（編成 1 は `k ≥ 1.022`、編成 2 は `k ≤ 0.979`）。
   - **【確定】カードの Lv はスキルの数値を変えない。** 素値が上限の 64% 程度でもアクティブ欄が 8 編成すべて一致した。Lv が効くのはパラメータと、それを介した「N人」の対象選抜だけ。
   - **baseline（支援 0 のリーダー）は 2 件ともモデルと完全一致**（パッシブ 2.4 / 8.8）。`ΔP` も総量 `T = 0.60 × A + ΔP` も正しい。
   - **決定的な事実: リーダー支援を足すとパッシブ欄が動く**（編成 1 は 2.4 → 2.5、編成 2 は 8.8 → 8.6）。production は `W_P = ΔP` で配分が約分されるためパッシブ欄が動かない。3 欄の再配賦は実機で起きていて、**その重みだけが違う**。
   - **積み重ね系列 S1 / S2 / S3 で作業仮説が支持された**（2026-09-15）。支援 0 のリーダー側は 3 系列とも完全一致し、`W_P` の必要倍率が同じメンバーへの支援量に対して単調に下がる: 11% → 0.96〜1.01、22% → 0.951〜0.976、33% → 0.885〜0.903。総量 `T = 0.60 × A + ΔP` は全件で正しい。
   - 編成 1 だけ倍率が 1 を超えるが、`0.60 × A` が 0.1 の境界を 0.05 超えているだけの 1 quantum のケースで、**衣装欄を「アクティブ欄との差」として量子化すれば補正なしで実機が出る**。位相の問題として切り離す。
   - **次は S4**（ロボ子0 / レイネ0 / ビジュー0 / ネリッサ1 / スバル0）。`ΔP` は S2 とほぼ同じ 4.395 だが **4 人がそれぞれ 11% で積み重ねがない**。クロニーの衣装欄が **40.4 なら「積み重ねだけが逓減」、40.5〜40.6 なら「総量で逓減」**。ここが決まってから `W_P` の形を決める（自由係数の fit はしない）。
   - production は現行のまま（位相を替えても 編成 2 は説明できないため）。
   - 編成を選ぶときは **`A`・`0.6A`・`1.6A` の 3 つすべて**が 0.1 の境界から離れていることを条件にする（2026-09-15 の 編成 1・3 は `1.6A` が境界から 0.0005 しか離れず判定に使えなかった）。
   - **項目 3 との循環は残る**が向きが変わった: 位相が決まれば表示値から raw の区間を作る操作が確定し、`W_blue` の候補比較の前提もそろう。青なしの実験は `W_blue` を含まないので**先に片付く**。
   - production は当面 現行の独立・切り上げのまま（「5 欄で規則を 2 つ持たない」）。

## パラメータ・ボード

5. **赤割合効果の丸め単位**
   - 現実装は一部で±1〜2程度ずれる。Goldenを丸め仮説へ合わせない。
6. **コネクト効果**
   - 範囲、重複、割合・‰丸め、黄/赤/緑への適用、5凸時の扱いを実機確認する。
7. **開花途中の実数値**
   - **まだ本確認を通していないカードが残っている。** 進み具合（確認済みの枚数・強化前の区間の内訳）は [card-data-provenance.md](../../human/card-data-provenance.md) が正典。新しく転記する値は出所を残し、実機目視が取れたら `observed-text` へ昇格する。
   - **master の level 番号と凸段階の対応は不明のまま。** 2026-09-13 に「そら / ぼたんの実機 0凸 が master level 1 と食い違う」という反例を持っていたが、2026-09-15 にその 0凸 Active を「未確認」へ取り下げたので**反例は手元にない**。かといって一律対応を支持する証拠もないので、level 番号だけで未観測の凸値を確定扱いしないという扱いは変えない（`docs/human/card-data-provenance.md`）。
   - 解析コーパスに残る `unknown`: 恒常そら / ぼたん 0凸の Active だけ（`displayScoreCategoryCorpus.audit.test.ts` で列挙。値は最大側と同じなので評価には効かない）。2026-09-15 の実機確認 34 枚で、ほかの推定入力は消えた。
   - **実機で埋めるときの入口は開発用の「開花文言」**（サイドメニュー → 開発用。`src/components/BloomTextSheet.vue`）。カードを選ぶと区間ごとの文言が現在値で埋まり、出所が「未確認」の区間が埋めてほしいところ。**ピッカーには確認済み（`BLOOM_TEXT_VERIFIED_CARD_IDS`、2026-09-15 時点で 34 / 73 枚）を外したカードだけが出る**ので、残っているカードがそのまま「文言は入っているが本確認がまだ」の集合になる。フォームはデータを書き換えず共有用データを出すだけなので、`cards.json` / `bloomEvidence.ts` への反映は従来どおり根拠を確かめてコミットする（`docs/human/card-data-provenance.md`）。

## 実ライブ・イベント

8. **イベントスコアボーナスの適用位置**
9. **実ライブスコアエンジン**

## データ・公開

10. **公開まわりの未確認（2026-09-14）**
    - OGP 画像（`public/og/holodori-optimizer.png`）の見出しが、サイトと同じ Zen Maru Gothic ではなくフォールバックのゴシックで焼かれている（作った環境から Google Fonts へ到達できなかった）。描き直すときは `scripts/og-image.html` を 1200×630 で開いて撮り直す。
    - 結果の共有は X の投稿画面とテキストのコピーまでしか実機で確認していない。**Web Share API（共有シート）の経路は iOS Safari / Android Chrome で未確認**（Linux の Chromium に `navigator.share` がない）。
    - X / Facebook のカードでの見え方も未確認（公開後でないと確認できない）。
11. **0 件になった理由が画面から分からない（2026-09-14）**
    - しぼりこみで 0 件のとき、案内文は「しぼりこみを外して再度さがしてください」までで、**どのチップが効いているかを言わない**。
    - 所持カード 5 枚だとメンバーの組合せが 1 通りしかなく、パッシブの条件 1 つで 0 件になる（原因と実例は `.claude/rules/engine-structure.md`）。ユーザーが実機で当たった。
    - 案: 0 件のときにチップを 1 つずつ外して再探索し、原因のチップ名を出す / 「しぼりこみを外してもう一度さがす」ボタンを置く。**どちらを採るかはユーザー未回答。**
12. **権利ガイドライン原文の人間確認**
13. **初期データの抜き取り実機照合**
14. **仮想ガチャの未確認★3/★4・ダイヤパック**
