# 開発計画: ホロドリ パーティ編成最適化ツール

『hololive Dreams』のユニット編成(リーダー 1 + メンバー 5、リーダーとメンバーは重複可)を最適化する静的サイト。設計判断は ADR-001〜003、ゲーム仕様は [docs/human/game-spec.md](../../human/game-spec.md)、権利の枠は [docs/human/rights-policy.md](../../human/rights-policy.md)、UI 制約は `.claude/rules/ui-design.md` が前提。

## 完了サマリ(Phase 0〜4.5、2026-08-31)

- **Phase 0 調査・方針**: ゲーム仕様・既存ツール・権利関係・Vite+ を調査し ADR-001〜003 と docs 骨格を作成([事前調査](../../human/tmp/20260831-prior-research.md))
- **Phase 1 足場**: `vp create`(vue-ts)+ pnpm 11.24.0、ci.yml / deploy.yml、LICENSE (MIT)。main マージで https://hoshock.github.io/HolodoriOptimizer/ に公開済み
- **Phase 2 データ層**: スキーマ(raw + structured 二重持ち)、★5 全 70 枚・楽曲 181 曲・ホロメン 54 名・所属 15 件、整合性検査を `vp test` に組込み。2026-08-31 のスキーマ拡張(`self` 対象・スコアサポートの効果単位条件)で全 4 スキル種とも構造化率 100%(テストで強制。複合衣装文の解釈は `src/data/meta.json` の notes)
- **Phase 3 エンジン**: スコアモデル + 全探索(リーダー固定 / 一部固定 0〜5 / 除外 / TOP n、同一ホロメン排他はメンバー間のみ)。1,124 万通り約 5 秒、Web Worker + 進捗 + 中止
- **Phase 4〜4.5 UI**: カードピッカー(検索 + 所属単一選択チップ + タイプセグメント、1 行 1 枚でスキル表示)、リーダー/メンバー枠スロット(固定寸法・タイプ淡色面・スキル常時表示)、除外チップ一覧、結果ランキング(金銀銅+タップで詳細モーダル)。7 回のユーザーフィードバックを経た制約は `.claude/rules/ui-design.md` に集約(モバイルファースト、390px 実測確認)
- **所持カードモード (2026-08-31)**: ページ内タブ「全カード / 持っているカード」を追加し、登録した所持カードだけを対象に最適化するモードを実装。編成フォームは `OptimizerPanel.vue` に共通化し、プールの絞り込みは除外リストの補集合で実現。所持カード ID は localStorage に保存(Phase 5 の保存項目を前倒しで実施)。所持カードモードのみリーダー省略可でリーダーも探索する(エンジンの `leader: null`。全カードモードは探索空間が大きすぎるためリーダー必須 — 2026-08-31 ユーザー指定)
- **スキーマ拡張・構造化 100% (2026-08-31)**: `BuffTarget` に `self`、`ScoreSupportBuff` に効果単位の `condition` 上書きを追加して残 27 件を構造化。詳細モーダルの状態バッジを廃止し、不発動スキル行のグレーアウト表示に統一
- **仮想ガチャ (2026-08-31)**: ヘッダ右上のアイコンから開く仮想ガチャシミュレーション(**ピックアップガチャのみ**、10 回の 10 枚目は★4 以上確定枠、有償ダイヤのみ・課金額表示、購入は確認済みの 9,800 円パックのみ)。UI はピックアップ枠(UnitSlot と同形のプレースホルダ)+ 1回/10回 の等幅ボタンで説明文なし。通常・初心者応援・スタートダッシュ・無償ダイヤ・割引単発・天井 Pt は再現しない(2026-08-31 ユーザー指示)。仕様の根拠は [docs/human/game-spec.md](../../human/game-spec.md)「ガチャと課金」。★3/★4 は「1 ホロメン 1 枚」仮定でホロメン名表示(pending 5)。最適化ツールは★5 のみで不変。あわせてヘッダのサブタイトルを削除

## 完了サマリ（期待値最適化+UI 統合 Step 1〜3、2026-09-01）

- **Step 1 タブ統合**: タブ廃止・`OptimizerPanel` 単一化。ステップは 1 さがす対象（セグメンテッドコントロール「全カード | 持っているカード」+カードを選ぶ行ボタン、状態は `holodori-optimizer:search-all`）/ 2 除外（所持と独立）/ 3 リーダー / 4 メンバー / 5 曲 / 6 さがす。メンバー枠は先頭の空きのみ有効（解除で上詰め）、使えない操作は入口ボタンで disabled
- **Step 2 期待値エンジン**: 仮定値は `src/data/live.ts`、モデルは `src/engine/live.ts`（寄与=効果量×期待カバー時間/曲長、リーダー枠は発動しない前提、カード単位で前計算）。総合期待スコア = unitScore × (1 + active + sp)。詳細モーダルは検算できる内訳（絶対値主体・%は補足、和が見出しと一致、パラメータ表に合計行+変化なしセル淡色）
- **Step 3 曲別最適化**: 曲スロット+検索つき `SongPicker`（181 曲）。worker が songId → 曲長を解決（未指定は全曲の演奏時間の中央値）。コンボ数は比率モデルでは順位に影響しないため未使用
- **リーダー常時おまかせ（2026-09-01）**: 探索を「組合せ 1 回列挙 × 衣装スキル同型クラス評価+最大倍率の上限枝刈り」に組み替え（`optimize.ts`）、全カード×リーダーおまかせ 7.9 億通りが約 4 秒。実行不可はプールが空のときのみ
- **UI 刷新（2026-09-01）**: 見出しは名詞ラベル・件数は行ボタン内の値・任意スロットは既定値「おまかせ」を実値表示。スキル・役割バッジは正円アイコンに統一（`SkillIcon.vue`、ラベルは aria-label）。結果一覧はリーダー行を常時表示。規則は `.claude/rules/ui-design.md` に集約済み
- **結果表示の刷新（2026-09-01）**: 候補数入力を廃止して常に上位 100 件を計算し、結果は 10 件ずつ逐次表示（「さらに10件」）。「詳細 ›」等の誘導テキストを廃止し行全体の :active で示す。リーダー行は無彩色バンド+最右列に衣装アイコン（リーダー指定時はピンも）。アイコンは開花=最右・ピン=その左の固定列
- **開花（凸）対応（2026-09-01）**: スキーマに `bloomVariants`（確認済み段階の実文言・構造化必須）を追加し、worker が（カード, 開花段階）を解決してから探索（コアは無変更）。段階仕様=1凸アクティブ/2凸パラメータ/3凸SP/4凸パッシブ/5凸コネクト（ユーザー確認済み）。実数値が非公開のため未確認段階は仮定倍率（`src/data/bloom.ts`: パラメータ×1.05・スキル×1.1）で割り戻し。UI は所持ピッカーの登録済みカードに −花＋ ステッパー、全カード時は常に 0凸。表示は花びら+数字アイコン（0凸も常時）
- **ピッカー操作性（2026-09-01）**: 所持・除外に状態フィルター「すべて | 登録済み/除外中」+絞り込みの保持（閉じても復元、種別ごと独立）。単一選択（リーダー・メンバー・曲）は選択中の項目を開いた瞬間にリスト先頭へフィーチャー。開花ステッパーの上下限は押下ごと無効化

## 完了サマリ（SEO・保存層・おかゆモード、2026-09-01〜02）

- **ピッカーのフォーカス（2026-09-01）**: モーダルを開いたときの自動フォーカスを検索入力からシート本体（`tabindex="-1"`）へ変更（モバイルでキーボードが勝手に出ないように）
- **SEO・改名（2026-09-01〜02）**: 表示名を「ホロドリ編成お助けツール」に改名（検索キーワード「編成」を名前に含める。表記は「ホロドリ」第一）。title / description 刷新、canonical・OGP・twitter:card、`public/robots.txt` / `sitemap.xml`、Google Search Console の確認メタタグを追加。OGP 画像は公式素材が使えないため未設定（自作画像は pending 8）。インデックス登録の経過は pending 8
- **保存層の後方互換（2026-09-02）**: 所持カードの読み書きを `src/storage/owned.ts` に切り出し（旧 2 形式+版番号つき新形式を読む・未知 ID を捨てない）、`src/data/published-ids.json` でカード・ホロメン ID を凍結してテストで削除・改名を検知。規則は `.claude/rules/storage-compat.md`
- **おかゆモード（開発者のお遊び機能、2026-09-02）**: フッター最下部右のおにぎりで ON / OFF（URL パラメータなし・保存しない・ON で先頭へスクロール）。ON 中はページ全体を紫に、リーダーはおかゆんのカード（ピッカーでおかゆん以外 disabled、おまかせならおかゆんの中から探索）、メンバーにもおかゆんが 1 枚（4 枚選んでもいなければ最後の枠が「おかゆん（おまかせ）」+おかゆん以外 disabled）、除外不可。エンジンは `leaderCandidateIds` / `requiredMemberHolomenIds` を汎用オプションとして追加（未充足数 > 残り枠で枝刈り）。結果はおかゆんの行のピン列におにぎり、おまかせで入ったおかゆんの枠位置は候補ごとにランダム。持っているカードモードでは、おかゆんを登録するまで所持ピッカーのボタンを「おかゆんを選んでください」にしリーダー・メンバー・実行を disabled（エラー表示ではなく方針として案内）

## 完了サマリ（スキル発動のしぼりこみ、2026-09-05）

- **Step 6 のしぼりこみ**: 実行ボタンの上に「衣装スキル発動」「パッシブ全員発動」のピル形チップ（複数選択・既定は両方 ON・`holodori-optimizer:skill-filters` に保存）。エンジンは `requireCostumeSkill` / `requireAllPassives` を葉（5 人確定）で判定して不発の編成を候補から外す（未構造化は判定不能なので残す。探索コア・枝刈りは無変更）。6 枠すべて固定（この編成のスコアを試算）では適用せず不発バッジで理由を見せる。0 件は「しぼりこみを外して再度さがしてください」。導かれた規則（既定はおすすめ状態・隣接する選択コントロールとアクションは形で分ける・差の意図を提案時に明示）は `.claude/rules/ui-design.md` に統合済み

## 完了サマリ（曲ピッカー刷新+楽曲追加、2026-09-05）

- **曲データ**: `artists`（歌唱アーティスト名。ホロメン名またはユニット名）を全曲に追加し、ゲーム内配信順 No.182〜193 の 12 曲を追加して全 193 曲（出典は meta.json。easy〜hard のコンボ数が未確認の曲は `combo: null`）
- **曲ピッカー**: 検索を曲名+ホロメン名に拡張、所属チップ（アーティスト→ホロメンの所属、ユニット名は対応表）+「すべて | オリジナル | カバー」セグメントをカードピッカーと同じ部品で追加。絞り込みは閉じても保持。行は左=曲名+アーティスト、右=演奏時間（試算に効く値）+ EXPERT Lv の 2 列
- **曲ピッカーの並び順（2026-09-05）**: 「長い順 | Lv 高い順」の 2 分割セグメント。選択中を再タップで向きが反転しラベルが「短い順」「Lv 低い順」に変わる（向きはキーごとに記憶、閉じても保持）。選択中には現在の向きを ▼/▲ の片方で添える。並び順のキーの値を行の右列上段に強調表示。既定は長い順、同値はゲーム内の並び
- **Step 5 の未指定表記**: 「おまかせ」→ 左「指定なし」右「中央値の実値（2:09）/ 全曲の中央値」。曲は選ばれず中央値で試算する実態に合わせた（充填時と同じ 2 列）

## 完了サマリ（一覧のあいうえお順+メイン/ピッカーの部品統一、2026-09-05）

- **あいうえお順**: `holomen.json` / `cards.json` に読み `reading`（ひらがな手入力、`validate.ts` が検査）を追加し、カード一覧をホロメン名 → カード名の読みで並べる（`labels.ts` の `readingSortKey` が長音を母音化）。検索も読みに一致
- **部品統一**: メイン画面の枠 `UnitSlot` はピッカーと同じ `CardTile` を包むだけにし、曲は `SongRow.vue` を曲ピッカーと Step 5 で共用。効果テキストは 2 行固定（3 行案・縦送り案・3 行固定案を却下）、行の隙間 2px、名前ブロックと効果の間隔 9px で縦余白を対称に。メイン画面はパネル、ピッカーは白い 1 枚のシートのまま（帯・角丸ブロック・ステッパー・ピッカーのカードスタイルを試して却下）。曲名のスクロール表示は 3 形態を試して廃止
- **モーダル**: 出口は ✕ 1 つ（20px、全モーダル）。複数選択の「完了」とヘッダの件数表示は廃止。ヘッダはページヘッダと同寸（77px）。結果一覧は名前+サブタイトルを隣接、メンバー間 6px・パネル上下 10px。全 13 コミット（00b9343〜3827652）をデプロイ済み

## 完了サマリ（パラメータ計算仕様の検証+青ボード入力+Step 0、2026-09-06）

- **計算仕様の整理と検証**: ユーザーの実機観測（カード詳細 P/T/S の成り立ち、青ボード 31 マスの構成、アカウントの現在値、実測カード 4 枚）を、変わらない事実は知識スキル `parameter-calculation`（+ `references/blue-board.md`）、時点情報は常設の `docs/ai/tmp/status.md` に分離。実測 12 値を式で再構成して cards.json の stats が「レベル最大・2凸以上・ボードなし」の本体値と確定（±1）。2凸の割り戻しを 1.05 → 1.10 に修正
- **青ホロメンボードの入力と反映**: 全ホロメン共通の 31 マス（座標・効果・大マス、`src/data/blueBoard.ts`）、4 近傍の連結で「未解放を押すと経路ごと解放・解放済みを押すと先も解除」、左型/右型、すべて解放/解除、効果表。実機の形に忠実（正方格子・大マス 1.5 倍・丸角四角の接続点・記号 A/P/T/S/率/頻）。保存は `holodori-optimizer:blue-boards`（`src/storage/boards.ts`）。反映は P/T/S = 本体 + 固定値 + ceil(割合)、発動率・頻度はアクティブ期待値へ（仮定の式）。解決は `src/data/resolve.ts` の 1 本（Worker・表示・詳細の検算）。当初は両モードで効かせたが、同日夕方に「持っているカードで『ボード状況を考慮する』が ON のときのみ」に変更（2026-09-08 に、考慮しないときは全ホロメン全解放として試算する扱いへ）
- **Step 0「アカウント」**: 左から ホロメン（`HolomenPicker.vue`: 五十音順 / 解放数でソート、検索・所属チップ、行 56px → `BoardSheet.vue`）/ メンバー（持っているカードと開花。開花はここでのみ変更）/ ユニット（お気に入り編成、disabled のプレースホルダ）。Step 1 はセグメントのみ。メンバーピッカーは開花アイコンだけ。おかゆモード未登録時はボタン下に赤い 1 行（例外的処理）。並び順・絞り込みは閉じても保持・再読み込みでリセット
- **iPhone 実機の不具合対策**: 連打ズームの抑止（`touch-action: manipulation`）、モーダル閉じ後の最下部余白対策（実機未検証 — pending 9）

## 完了サマリ（Step 統合+探索オプション、2026-09-06 夕方）

- **Step 1 に除外を統合**: 旧 Step 2「除外するカード」を「さがす対象」のセグメント下の行ボタンにし、以降を Step 2 リーダー / 3 メンバー / 4 曲 / 5 さがす に繰り上げ。ボードの見出しは「ホロメンボード（青）」
- **探索オプション（Step 5）**: 「ボード状況を考慮する / 開花状況を考慮する / 衣装スキル発動に限る / パッシブ全員発動に限る」の 4 チップを 2 列 × 2 行で常時表示（折りたたみ+「オプション」見出しは試して撤去）。全カードではボード・開花を考慮せず（登録値を見ず、開花最大・ボード全解放のマックスとして試算 — 2026-09-08）上 2 つは未選択+disabled、持っているカードでは保存値どおり（既定は両方 ON）。保存は `holodori-optimizer:search-options`（旧 `skill-filters` を読み込みで引き継ぐ）。メンバーピッカーの開花アイコンは開花を考慮するときだけ
- **さがすボタンの色**: 実行専用トークン `--action`（緑 #1c8c6e）。おかゆモードは紫のまま

## 完了サマリ（スピナー+サイドメニュー+ボード 4 色、2026-09-07）

- **実行中の表示**: 進捗バー・評価件数・中止ボタンを撤去し、実行ボタン内の白いリング（スピナー）だけで示す（ボタンの寸法・色は保つ）。`useOptimizer` の progress / cancel はエンジン側に残したまま未使用
- **サイドメニュー**: ヘッダ右上のガチャアイコンをハンバーガーに置き換え、右からのサイドバー（`SideMenu.vue`）に「仮想ガチャ」「ソースコード（GitHub）」をアイコン左+ラベルで置く。上端はヘッダ下端（開く瞬間に測る）、地は半透過+ぼかし、ハンバーガーは開いている間 同じ場所で ✕ に変わる。scrim タップ・Escape でも閉じる。`useModalChrome` にライフサイクル非依存の `acquireModalChrome` を追加
- **サイドメニュー第 2 弾（同日）**: 共通の閉じるボタン `CloseButton.vue`（ハンバーガーの ✕ と同形）を全モーダルへ。メニューに「カード一覧」「曲一覧」を追加（既存の `CardPicker` / `SongPicker` を一覧として開き、選ぶと `CardDetail.vue` / `SongDetail.vue` を重ねる。データにない項目は「未確認」）。おかゆモードの入口をフッタからメニュー最下部（固定行、ラベル「絶対おかゆんモードを ON / OFF」）へ移し、フッタは免責のみ。地の透過を 66% に
- **同日の訂正**: サイドメニューの地を不透過に戻す（scrim は維持）。シート内の注記を末尾の脚注（`.footnotes` / `.fn` / `.fn-num`、※n の 2 列）へ移す。カード詳細の見出しを「カード一覧」に、色つきカードはタレント名+サブタイトルのみ、所属は下のタグ、順序はパラメータ → 衣装スキル → スキル → 開花 → コネクト効果。結果詳細のヘッダを 77px/24px に揃え、iOS で青く出ていた結果一覧の数字は `button { color: inherit }` で解決
- **ホロメンボードの 4 色**: 見出しを「ホロメンボード」にし、名前行の右に赤・青・黄・緑のセグメント（左からゲーム内の順。青のみ有効、他は disabled）。解放マス・接続線は選んだ色（`--board-*` トークン）で描く。左型/右型のセグメントを廃止し、ホロメンごとの固定データ `holomen.json` の `board`（`blueSide` / `lifeSide`、全 54 名 — 2026-09-07 ユーザー共有）から左右を引く。保存の `mirrored` は読み飛ばし（`boards.test.ts`）

## 完了サマリ（緑ホロメンボード、2026-09-07）

- **実機確認**: ユーザーの引き継ぎメモ（配置・効果の仮説）を、実機確認 2 回で確定（1 回目: 下・中心のコネクトは共通で色の概念なし・24 マスの位置・配置差なし。2 回目: 接続線は 4 近傍で 09・10 の列も縦に繋がる・大マス 11/14/17・効果と所属別の値・フブキ）。仮説はリポジトリ外に留め、確定分だけを `references/green-board.md` に書いた。解析由来のデータは参考程度・必ず実機確認（ユーザー判断）
- **実装**: グラフ操作を `src/data/boardGraph.ts` に共通化し、`src/data/greenBoard.ts`（24 マス・所属別の値・フブキの例外・上限 900・アカウント全体の合計 `accountGreenEffects`）を追加。保存は `holodori-optimizer:green-boards`（青と同じ封筒、色ごと別キー）。`resolveCard` に緑（アカウント合計）を追加し、Worker・スロット・詳細の検算が同じ 1 本を通る。`BoardSheet` は色で盤面（青 11×7 / 緑 7×11、緑は反転なし）と効果表（全員の P/T/S・所属向け・解放した報酬系）を切り替え、緑タブを有効化。ホロメン一覧の解放数は青 + 緑
- **入力の置き場**: 緑は Step 0 のホロメン → ボードの緑タブ（アカウント共通の値だが「どのホロメンのボードのどのマスを解放したか」を写す形なので、ホロメン単位の入力で自然に集まる）。所属ボーナスの別入力は不要になった（緑ボードの所属向けマスがそれ）
- **同日の訂正（5 往復）**: マス記号を「グ」（所属向け）「酬」（報酬系）に。効果表は固定順で 12 行を常時表示（報酬系は未解放でも +0.0%）。名前を 1 行にし、その下の行に左 = 色の 4 択・右 =「解放 | 説明」の 2 択。説明モード（タップしても状態を変えず、選んだマスに濃色の輪、効果文言を「すべて解放 / 解除」と同じ高さの枠線なし淡色の帯に、文言の前に選んだマスの縮小、マス以外のタップで解除、直前の選択は色ごとに記憶）。盤面の描画領域に 4px の余白（端の大マスの輪が切れない）。本文（縦 flex + overflow）で overflow hidden の子が高さ 0 に縮む問題を `flex-shrink: 0` で解消。サイドメニューのおかゆモードのアイコンを丸なしのおにぎり単体に。導かれた規則は `.claude/rules/game-facts.md`（新設）と `ui-design.md` に統合済み

## 完了サマリ（さがす対象をオプションへ、2026-09-08）

- 旧 Step 1「さがす対象」（全カード / 持っているカードのセグメント + 除外するカードの行ボタン）を廃止し、Step 4「さがす」のオプション（「オプション ▼」で既定は畳む）の 1 行目に「持っているカードのみからさがす」（既定 ON。OFF でボード・開花のチップは未選択 + disabled）を置いた。ステップは 1 リーダー / 2 メンバー / 3 曲 / 4 さがす。除外するカードは入口を UI から外し、状態と探索への反映は保持（`excludedIds`・除外ピッカーのコードは残す）。保存キー `search-all` の既定を「持っているカードのみ」に

## 完了サマリ（赤ホロメンボード、2026-09-08）

- **実機確認**: ユーザーの引き継ぎメモ（解析由来の 63 マス候補。3 エリアで扱う指定）を game-facts.md の手順どおり 4 回で確定（1 回目: 全景と左右・上エリアの配置と大マスと線 → 2 回目: 上エリアの効果・ライフ系の配置 → 3 回目: ライフ系の効果（スキル文言も）・ステータス系の配置 → 4 回目: ステータス系の効果・仕様（リーダー時のみ、「全員」はメンバー 5 人でリーダーを含まない、提示値は増幅前）・記号案）。候補が全部一致。仮説はリポジトリ外に留め、確定分だけを `references/red-board.md` に書いた（途中は「確認中」として段階的に push）
- **実装**: `src/data/redBoard.ts`（lifeSide = left 基準の 63 マス・`area`・効果 8 種・`redUnitEffectsByHolomen`・記号 A/P/T/S/支/命/判/回/経/金）+ テスト。保存は `holodori-optimizer:red-boards`（`BoardColor` に `red`）。エンジンはリーダー依存なので Worker → `optimize` の `redByHolomen` に渡し、衣装スキルの同型クラスの鍵に赤（固定値・割合）を含めて葉で (t_p + 固定値 × Σ(1 + パッシブ%)) × (1 + 割合) と評価、枝刈りの上限にも赤の最大値を入れる。`computeUnitScore` に `red` と内訳 `redTotals` を追加し、結果詳細に列「赤ボード」（見出しの「〜後」は全列から外した）。BoardSheet の赤タブ: 3 エリア（左 / 上 / 右）を出口（◀左 / ▲上 / 右▶ — 枝が画面外へ続く位置、接続線も出口まで）で移動、マス間隔は赤だけ 40px、効果表 13 行（歌唱者条件の〜は短い表記、値は nowrap）。ホロメン一覧の解放数は 4 色の合計
- **同時にやった表記統一**: ボード効果の割合を全色で小数第 1 位に（`formatBoardPercent` / `formatBoardPermil`）。正典 blue / yellow の表記も揃えた

## 完了サマリ（黄ホロメンボード、2026-09-08）

- **実機確認**: ユーザーの引き継ぎメモ（解析由来の 31 マス候補）を、game-facts.md の手順どおり実機確認 2 回で確定（1 回目: 形は青の左右反転と同じ 31 マス・内側のコネクトは中心から 7 マス目・大マス 7 個・縦横は全部繋がり斜めなし。2 回目: 位置番号 → 効果の候補表が全部一致、フワワ・モココのソロ系は「楽曲歌唱者が FUWAMOCO のみの楽曲」）。仮説はリポジトリ外に留め、確定分だけを `references/yellow-board.md` に書いた。適用仕様は同日に追加確認（下）
- **実装**: `src/data/yellowBoard.ts`（右型の座標で 31 マス・楽曲スコアボーナス 3 種とホロワーク報酬 3 種・FUWAMOCO の例外・記号 ソ/ユ/全/レ/キ/特）+ テスト。保存は `holodori-optimizer:yellow-boards`（`BoardColor` に `yellow`）。BoardSheet の黄タブを有効化（11×7、青が右のホロメンは x 反転、黄は文字を濃色に、効果表は 6 行固定順）。ホロメン一覧の解放数は青 + 黄 + 緑。試算への反映は同日の追加で（下）
- **黄の左右**: `holomen.json` に持たず `blueSide` の反対で導く（`isYellowLeft`）
- **同日の追加**: 緑の所属向けマスの記号を「グ」→「ユ」に。楽曲スコアボーナスの上限 10.0%（`YELLOW_SONG_BONUS_CAP_PERMIL`）。楽曲の区分（ソロ = ホロメン 1 人 / ユニット = 複数人 / 全体 = hololive IDOL PROJECT）を `songSingers.ts` が `artists` から導く（所属名のアーティストは所属の全員、Blue Journey・不知火建設は歌唱者未確認 null）。スコアへの反映: 「編成外でも効く（はい）」「式の位置はわからない」「コネクト増幅はある」→ `accountYellowEffects` / `yellowSongBonusPermil`（上限 10%、FUWAMOCO は 2 人のみの曲で 2 人分合算）を Worker で曲ごとに計算し、`LiveParams.songBonus` として総合期待スコアに (1 + X%) を掛ける（仮定。順位は変わらない）。結果詳細に「楽曲スコアボーナス（黄）」の行。歌唱者の曲別データ（なかま歌・また傷に触れる）は `SONG_SINGER_OVERRIDES`

## 完了サマリ（カルーセル・:active 削除・改名、2026-09-08）

- **PageCarousel**: メンバー枠 5 つと結果一覧（上位 10 件。100 → 10）を `PageCarousel.vue` で 1 つずつ表示。下に「n / N」と左右の三角（端は disabled）、パネル全体の左右スワイプと PC のマウスドラッグでも送る。7 往復の訂正: 分子が揺れる → 送った瞬間に確定 / スワイプ直後にタップできない → click 抑止は同じジェスチャだけ / 止まるまでが遅い → scroll-snap をやめ自前送り（指に追従、離した瞬間に確定、transform 300ms。180ms は早すぎ）/ ちらつく → 前後 2 ページだけを個別に transform し will-change で常時レイヤー（実機で消えたことを確認）。規則は ui-design.md に統合済み
- **:active の全削除**: 結果行の暗転が「不要」→ iOS はタッチリスナーのある要素にしか :active を当てないため実機では元から見えていなかったと説明 → 9 か所すべて削除。ui-design.md の規則を置き換え
- **改名**: ID1期生 / ID2期生 / ID3期生 → AREA15 / holoro / holoh3ro（表示名・スキル文言・アーティスト表記・所属対応表。ID は据え置き）
- **追補（同日）**: カードを入れたあとの「次の枠へ」はアニメーションなし（`PageCarousel.jumpTo`）。結果詳細の内訳で丸め誤差を楽曲スコアボーナス行に寄せていて曲未選択に「+-1」が出た → 誤差はアクティブ期待値の行へ、楽曲行は base × songBonus の丸め

## 完了サマリ（総合力モデル — 実機内訳の加算モデル、2026-09-08）

- **モデル**: ゲームのユニット編成画面の「総合力」= メンバーパラメータ + 衣装スキル + ホロメンボード効果 + パッシブスキル + メモリー効果 + メンバー強化ボーナス を `src/engine/power.ts` で再現（ADR-004）。割合効果は素値（`Card.naturalStats`）基準でパラメータ単位に切り上げ、「◯◯2人の」は対象パラメータの上位 count 人、赤は固定値 × 5 + 素値合計 × % 合算、強化ボーナスはメモリー抜き。旧 `computeUnitScore` の連鎖乗算は廃止
- **探索**: `optimize.ts` の高速評価器を同じ中核関数に載せ替え（順位づけ = 総合力 × 実曲期待値の倍率。全候補の順序と値が詳細計算と一致することをテスト）。Worker にアカウント補正を渡す
- **UI**: Step 0 にメモリー % ・強化ボーナス % の入力（`holodori-optimizer:account-bonus`）。結果詳細の 2 番目の表をゲーム画面と同じ 6 項目 + メンバー別（素の P/T/S と総合力）に
- **データ**: 5 枚の開花途中の実文言を `bloomVariants` に記録。`live.ts` の発動確率を外部解析の有力値 55/45/35 に
- **ゴールデン**（`power.test.ts`）: パッシブ・メモリー・赤・強化ボーナスは完全一致。素値の復元誤差（pending 7）で メンバーパラメータ −2・衣装 +2 / −1・ボード +2・総合力 +2 / −1
- **表示スコアボーナス（同日追補）**: ユーザー指示「多少ズレてもいいから仮定で実装」で引き継ぎメモの仮定を `src/engine/displayScore.ts` に実装（B +3.8pt / A +10.1pt）。結果詳細は メンバー別 → 総合力 → スコアボーナスの順（脚注も出現順）、結果の値と順位づけは ユニットスコア（試算）× (1 + 黄) × (1 + イベント)（同日「結果の値は最終的なユニットスコア値にしろ」）。モデルは同日中に次の項目のタイムラインモデルで置き換えた
- **表示スコアボーナスのタイムラインモデル（同日、2 回目の引き継ぎ）**: 20 ケースの実測（status.md）をゴールデンに `displayScore.ts` を作り直し（5 人共通の 200 秒タイムライン・確率 55/46/37・青は確率加算と周期除算・パッシブは供給側確率で重みづけした差分・SP はアクティブ欄に比例 + 発動率 UP の増分・K = 2.03734 で ceil）。アクティブ ±0.1・SP ±0.5・合計 −2.6〜+0.9pt。アクティブの追加条件と SP の発動率 UP を `conditionalScoreUp` / `skillRateUp` に構造化（72 件）。探索は上限値で shortlist を集めて正確評価する近似に（`SHORTLIST_FACTOR` / `SHORTLIST_MIN`、ADR-004 更新）

## 残作業

- [ ] **イベント獲得ボーナスの UI（設計やり直し）**: データ・型・計算関数（`src/data/events.json` / `src/engine/event.ts`）は 2026-09-08 に整備済み。獲得ボーナス最大化（`optimize` の `objective: "eventBonus"`、さがすの目的セグメント、結果の主数値「+NN%」、詳細の内訳）を同日に一度実装したが「微妙だ」で main から revert し、ブランチ `claude/event-bonus-objective`（103ba3d）に退避。**何が微妙だったかを聞いてから**案を出し直す（実装は指示を待つ）。課題曲の +10% は UI に出さない（ユーザー判断）。未確認事項は pending 11
- [ ] **ユニット（お気に入り編成の記録）**: Step 0 の 3 つ目のボタン。ホロメンごと（リーダーごと）にお気に入りのユニット編成を記録する機能。仕様はユーザーから（2026-09-06「ボタンだけ作っておいて」）
- [ ] **コネクト増幅・黄の式の位置**（pending 4）: 4 色とも入力・試算に入った。コネクトマスの増幅は倍率不明で未対応。黄の楽曲スコアボーナスは総合期待スコアに (1 + X%) を掛ける仮定。赤の式は 2026-09-08 に実機内訳で確定（固定値 × 5 + 素値合計の割合、掛け合わせなし）
- [ ] **Step 4 ボード青マス配分**: 仮定値で、候補ごとに限界効用の大きい順の開放配分（誰に何個で総合スコアがいくら伸びるか）を詳細モーダルに提示。青ボードの入力ができた今は「登録済みのボードからの追加解放」として設計し直す。**着手はユーザーの指示待ち（2026-09-01「いったんストップ」）**
- [ ] **表示スコアボーナスの精度向上**（pending 12）: 2 回目の引き継ぎでタイムラインモデルに作り直し、20 ケースで 合計 −2.6〜+0.9pt（ユニットスコア −1.2%〜+0.4%）。アクティブ ±0.1・SP ±0.5・K は全一致。残るのは青の式（頻度の山・発動率の効きすぎ）とパッシブの重みづけで、pending 12 の実機確認（みこ・マリン・フワワの青、マリン 1凸文言、頻度もう 1 段、発動率だけの差分）を待って式を差し替える

## Phase 5(将来): 拡張

- ホロメンボード補正の残り（コネクト増幅・黄と赤の式の位置。青は 2026-09-06、緑は 2026-09-07、黄・赤は 2026-09-08 に完了 — pending 4）
