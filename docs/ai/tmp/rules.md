# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし）

## Cases

- 2026-09-05 / Step 6 のしぼりこみチップ: 既定を「両方 OFF（従来どおり）」で入れたが、ユーザーは「両方 ON」を既定にした。読み取れる好み: 絞り込み系オプションは保守的な OFF ではなく、実用上おすすめの状態を既定にする（既定のまま押せば一番よい結果が出る）。想定 glob: `src/components/**/*.vue`
- 2026-09-05 / Step 6 のしぼりこみチップ: 選択状態（濃色地）の 2 チップと主アクション（濃色地）の実行ボタンが隣接し、既定 ON だと全部黒くなって「オプションかアクションか」が見分けられないと指摘。読み取れる好み: 同じ塗りの選択コントロールと主アクションボタンを隣接させない — 隣接させるなら形・サイズ・幅で役割を分ける。想定 glob: `src/components/**/*.vue`
- 2026-09-05 / Step 1 のセグメント（つながった 2 択）と Step 6 のチップ（8px 離した複数選択）の違いについて「意図が知りたい」と質問された。読み取れる好み: 既存パターンと形が違う UI を入れるときは、提案時にその差の意図（単一選択と複数選択の区別など）を明示する。想定 glob: `.claude/rules/ui-design.md`
