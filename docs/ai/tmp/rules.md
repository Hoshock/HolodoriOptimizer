# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 35 回目（2026-09-11 深夜、コネクト効果の手直しのあと）も候補なし。この回の好み（形の対を一緒に動かさない・虹色の輪は選択の輪と同じ幾何・一覧は純粋に形・図形は反転しない・保存データを壊さない）は同じターンで `ui-boards.md` / `ui-parts.md` / `storage-compat.md` / ADR-008 の Update へ直接統合した。34 回目（UI の手直し 4 件のあと）も候補なし。この回の好みはフィードバックと同じターンで `ui-boards.md`（赤の下エリア）/ `ui-parts.md`（コピーはアイコンボタン）/ `ui-flow.md`（サイドメニューの折り畳みグループ・Step 0 の 2 ボタン・お気に入りの入口）へ直接統合した。33 回目（赤の歌唱者条件の検証後）は候補なし。32 回目で 2 件を昇格: **派生する欄の値は表示済みの丸め値でなく raw 中間値から計算する**は `engine-structure.md` へ、**1 つの実機確定を別の未確定項目まで「解決済み」に広げない**は `game-facts.md` へ。31 回目で `skills-convention` へ 1 件（URL で渡すスキルは 1 ファイルで完結）、30 回目で `ui-design.md` へ 1 件、29 回目で 2 件、26 回目ぶんは `storage-compat.md` と `ui-parts.md` へ統合済み）

## Cases

- 2026-09-11（UI 9 件）: 「脚注に日付・実機観測の言及は利用者に不要」→ 同じターンで各脚注から削除（規則候補: **利用者向けの脚注に観測日・「実機観測に基づく」を書かない。根拠の日付はコード・ドキュメント側に置く**。同種は 1 回目なので事例として置く）。「絞り込みを 1 行に収める・下端に確認ボタン・全トグルを既定・戻りの出口は中央」は `ui-parts.md` / `ui-boards.md` へ直接統合済み。
- 2026-09-11（赤スコアサポート）: 「固定 0.88 × X は一般式として棄却」「0.85 への置き換えも禁止」「baseCandidateSeconds は青補正なし」「historical Golden と current account snapshot を区別」→ 同じターンで `game-facts.md` に「1 編成の観測にしか合わせていない定数を確定と書かない」として統合済み。読み取れる好み: 一致した点の数より**編成を変えた独立な 2 点目**を確定の条件にする、定数の差し替えより構造式への置き換えを求める。

（ほかは なし — 35 回目・34 回目も事例は各ルールファイルへ直接統合済み（上）。2026-09-11 の棚卸し 32 回目で 2 件を `ui-flow.md` へ例として統合: 「ユニット画面はゲームのユニット編成画面と同じ入力（曲なし）で出す」は Step 0 の項へ、「再実行中は前回の結果を薄く残して領域を空にしない」はスピナーの項へ。31 回目では 3 件を `ui-parts.md` / `ui-flow.md` / `ui-design.md` へ、30 回目では「スクショからの取り込み」の事例群を `ui-parts.md` / `ui-flow.md` / `structure-import` スキルへ統合済み）
