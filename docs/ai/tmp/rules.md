# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 2026-09-08 の棚卸し 20 回目で統合）

## Cases

（なし — 2026-09-08 の棚卸し 22 回目で `.claude/rules/ui-design.md`（設計相談は案の提示で止める・順位に影響の薄い要素は UI に出さない・除外は役割別の 2 入口・チップとボタンの同居・ラベルの短縮）と `.claude/rules/storage-compat.md`（除外の状態名）へ統合）

- 2026-09-08（ユニットスコア検証の引き継ぎ、追加指示）: 「未確定の SP 46.0% や Board/Passive の配賦式を、実機値に合わせるための場当たり的な定数・補正値でハードコードしない」「ゴールデンケースに一致しない場合は無理に合わせず、どの計算段階から何点ズレるかを特定して報告」「探索用 fast evaluator と詳細計算でロジックが乖離しないよう共通ロジックを使う」「Actual Song Score 系は削除せず Displayed Unit Score と責務を分離」。読み取れる好み: 実機再現は構造の理解を優先し、当てはめの補正を嫌う。同日に `.claude/rules/game-facts.md`（paths に `src/engine/power.ts` を追加）へ直接書いた — 棚卸しで置き場の妥当性を確認する。
- 2026-09-08（表示スコアボーナス）: 「多少ズレてもいいから別エージェントが言ってた仮定で実装して」— 直前の「場当たりの定数を入れない」より、画面に値が出ることを優先した。読み取れる好み: 未確定でも仮定を明示して数字を出す方が、出さないより良い（精度は実機確認で詰める）。結果詳細の並び: メンバー別の表を一番上、次に総合力、次にスコアボーナス（4 項目）。総合期待スコアの内訳表（アクティブ / SP 期待値・黄）は不要。
- 2026-09-08（結果の値と脚注）: 「脚注が上から132になってるのきもすぎ」「結果の値は最終的なユニットスコア値にしろ。総合期待スコア(試算値)という文もいらない。下のユニットスコア(試算)の行もいらない」→ ui-design.md に直接統合（脚注は出現順、主数値は 1 つで説明文なし）。「メモリーと強化ボーナスの入力 UI がださい。いい案ないのか」→ 設計相談として案の提示で止めた。
- 2026-09-08（アカウント補正の入力欄）: 2 列横並びの小さなラベル + 数値欄を「ださい」→ 案 3 つを出したところ「メモリーと強化ボーナスでそれぞれ 1 行にして。両端に揃えて。ラベルのフォントサイズはボタン内と合わせて」。読み取れる好み: 設定行は 1 項目 1 行・ラベル左端と値右端の両端揃え、文字サイズは同じパネル内の部品とそろえる（小さく淡くしない）。
