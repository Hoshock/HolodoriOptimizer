# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 2026-09-09 の棚卸し 26 回目で `.claude/rules/storage-compat.md`（保存するのは入力だけ・数値は表示時に計算し直す）と `.claude/rules/ui-parts.md`（取り消しのきかない登録操作は `ConfirmDialog` で確認）へ統合。文面は棚卸しで確認済み）

## Cases

- **2026-09-10 / 選択シートを閉じるかどうか（`src/components/OptimizerPanel.vue` + `src/components/CardPicker.vue`）**: 「メンバー所持は、ウィンドウ閉じることなく複数選択できる（Advent の 3 人をポチポチポチと）けど、編成メンバーの固定は毎回ウィンドウ閉じるのはちょっと気になってる」→ 所持ピッカーは `mode="multi"`（タップでトグル・閉じない・`memory-key` で絞り込みも保持）なのに、メンバー枠の固定は `mode="pick"` で 1 枚選ぶと閉じ、`memory-key` もないので開き直すたびに検索・所属の絞り込みからやり直しになる。読み取れる好み: **一度に複数入れるものは選ぶたびに閉じない**（閉じるのは 1 つだけ選ぶもの = リーダー・曲だけ）。固定は探索へ集合として渡る（`fixedMemberIds`）ので枠の番号に意味はなく、単一選択の作法になっているのは実装の都合。→ 案 B（固定そのものを複数選択ピッカーにする）をユーザーが選択し、同日実装（`ui-parts.md` / `ui-flow.md` に反映済み）。

（なし — 2026-09-10 の棚卸し 27 回目で `ui-parts.md`（結果詳細の並び・内訳は既定で畳む・主数値の行の脚注番号と操作の位置・脚注は初期表示では画面に出さない・同じ見た目の表を続けて置かない・数値は既存のピッカーで選ばせる・結論に使わない情報は出さない）と `ui-verification.md`（全画面に効く扱いは対象の全画面で実測する）へ統合。2026-09-09 ぶんは棚卸し 26 回目で統合済み）
