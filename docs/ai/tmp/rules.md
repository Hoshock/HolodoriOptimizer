# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 2026-09-08 の棚卸し 20 回目で統合）

## Cases

- 2026-09-09「現在の製品ロジックに使われていない旧経路は整理・削除する」— 順位づけにも UI にも使われていない旧モデル（曲長ベースの `src/engine/live.ts`）を `legacy` に改名して隔離するのではなく**削除**し、必要になったフェーズでゼロから作り直す方針が示された（「現在の簡易 live.ts を延命して改造する必要はない」）。あわせて型・関数の名前は実体に合わせて改名する（`expectedScore` → `adjustedUnitScore`、`liveFactorOf` → `scoreModifierFactor`）。読み取れる好み: 検証されていない近似を「エンジン」の名前で残さない・名前が実体とずれたまま放置しない。想定 glob: `src/engine/**`
- 2026-09-08「開花状況とボード状況を考慮しないときは、それぞれマックスの状態としてスコアを出したい」— 考慮しない設定の扱いを素の値（0凸・ボードなし）からマックス（開花最大・4 色ボード全解放）へ変更した。読み取れる好み: 未指定・考慮外の育成状態は最小ではなく到達可能な最大を仮定する（将来向けの探索は「育てきったらどれが強いか」を見たい）。想定 glob: `src/components/OptimizerPanel.vue`（`.claude/rules/ui-design.md` の該当文はこのターンで更新済み）
- （以前の事例は 2026-09-08 の棚卸し 23 回目で `.claude/rules/game-facts.md`（仮定を明示して数字を出す・信頼度の区分とケース別の誤差表で報告・仮定を個別にいじらない）と `.claude/rules/ui-design.md`（結果詳細の表の並び・設定行は 1 項目 1 行の両端揃え）へ統合）
