# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 2026-09-09 の棚卸し 26 回目で `.claude/rules/storage-compat.md`（保存するのは入力だけ・数値は表示時に計算し直す）と `.claude/rules/ui-parts.md`（取り消しのきかない登録操作は `ConfirmDialog` で確認）へ統合。文面は棚卸しで確認済み）

## Cases

- **2026-09-10 / 結果詳細の並び（`src/components/UnitBreakdown.vue`）**: 「ユニットスコアの下にリーダーパネルを出し、その下に仮想ガチャと同じメンバーコンポーネントの形で5人横並びで置く。リーダーメンバーという言葉は消す。でその下に表が続く形」→ 数値の表より先に「誰の編成か」を見せる並びへ変更し、役割名（リーダー / メンバー）はテキストではなく構造（全幅パネル / 5 列タイル）で示すようになった。読み取れる好み: **既にある部品の形を流用する**（ガチャの結果タイル）／**見出しの語で説明せず構造で伝える**。副作用としてメンバーのスキル原文が画面から消えた（`ui-parts.md` に反映済み）。
- **2026-09-10 / 発動頻度シートの削減（`src/components/FrequencyPlanSheet.vue`）**: 「冒頭の説明不要」「評価区間の文字が二つあるので片方削除」「秒数入力はしない。代わりに曲ピッカー」「現在から+ptは不要」「マスと追加のカラムは不要」「実効ではなく発動頻度」「％の数字の意味がわからない」「現在の設定も不要。他の案も不要」「不明な用語は脚注。上から順に」→ 決定に使わない情報・内部の単位・重複ラベルを全部落とし、用語と値の意味は脚注に回した。読み取れる好み: **結論（おすすめ）だけを本文に置き、意味の説明は脚注**／**入力は既存のピッカーで選ばせる**／**内部の単位（マス数・追加解放数）をユーザーの語彙にしない**（`ui-parts.md` に反映済み）。

（以前の記録は — 2026-09-09 の棚卸し 26 回目で `ui-parts.md`（詳細シートの送りは下端の固定エリア + スワイプなし・同じ操作は関係する全画面から・番号は全数を並べる・順位の円はフラット・重ねるアイコンは既存の列に合わせる・数字の隣は少し持ち上げる・実装ノート 2 件）、`ui-design.md`（元の部品の寸法を変えない・全項目を常に出す・`--chrome` の帯の上の正円は面色・調整ツールは対象を見ながら）、`ui-flow.md`（サイドメニューの構成とダークモード・管理用画面と認証を置かない理由・「何が空いているか」があるなら開けてよい例外）、`engine-structure.md`（解決 + optimize は 1 本）へ統合）
