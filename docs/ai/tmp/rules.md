# ルール候補（induction の記録先。housekeep で昇格・削除する）

## Rules

（なし）

## Cases

- 2026-09-05 曲ピッカーの並び順 — 4 分割セグメント（長い順 | 短い順 | Lv 高い順 | Lv 低い順）を提案したところ「昇順降順は同じボタンでトグルにしたい」。読み取れる好み: 並び順のキーと向きは 1 つのコントロールにまとめ、選択中のキーを再タップして向きを反転させる（選択肢の全列挙よりコンパクトさを優先）。ラベルは現在の向きを言葉で示す。続けて「トグルであることが伝わりにくい」→ 選択中のセグメントだけに反転の印（⇅）を添えたが「微妙。▲ と ▼の方がいい。片方だけ」→ 現在の向きを ▼（降順）/ ▲（昇順）の片方で示す形に。読み取れる好み: 再タップで状態が変わるコントロールは、ラベルの変化だけに頼らず記号を選択中に見せる。記号は「反転できる」抽象記号（⇅）でなく、現在の向きを表す ▲▼ の片方（想定 glob: `src/components/**`）

- 2026-09-05 曲枠の空状態の表記「おまかせ」にユーザーが違和感（「お任せの時に一曲選ぶのではなく、平均にしてるんだよね？」）。曲は選ばれず全曲の演奏時間の中央値（2:08）で試算しているので、「おまかせ」（ツールが選ぶ）は実態と違う。読み取れる好み: 「おまかせ」はツールが実際に最良を選ぶ枠（リーダー・メンバー）にだけ使い、代表値で計算する枠は「指定なし」+ 使われる実値を見せる。ui-design.md「既定値を実値まで具体化して見せる」の具体化候補（想定 glob: `src/components/**`）
- 2026-09-05 楽曲データの更新手順 — 出典 Ahoge0510/holodri-optimizer は 181 曲で止まっており、攻略サイト・公式サイトはこの環境から egress 不可。追加曲は、検索プロンプト（曲名・アーティスト・オリジナル/カバー・演奏時間・難易度別 Lv とコンボ・出典 URL を JSON で）をユーザーに渡し、別エージェントの結果から手入力した（12 曲、コンボ未確認は null）。次回も同じ流れになるなら手順として `src/data/README.md` かスキルへ昇格候補
