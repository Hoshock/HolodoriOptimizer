# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 2026-09-09 の棚卸し 26 回目で `.claude/rules/storage-compat.md`（保存するのは入力だけ・数値は表示時に計算し直す）と `.claude/rules/ui-parts.md`（取り消しのきかない登録操作は `ConfirmDialog` で確認）へ統合。文面は棚卸しで確認済み）

## Cases

- 2026-09-10「結果詳細画面でカードタップしたらカード詳細見れるようにして欲しい」: 編成の中のカード（リーダーのパネル・メンバーのタイル）も、一覧のタイルと同じくタップでそのカードの詳細へ行けるようにした。読み取れる好み = カードの表現はどこに出ていても詳細への入口（`ui-parts.md` に反映済み。ほかの画面のカード表現へ広げるかは次の同種の指示で判断）

- 2026-09-10「インポートする入り口のUIはどうする？」→ 案 A（サイドメニュー）を選択し、並びまで指定（「サイドバーの中では一番上。ソースコードのうえにさらにセパレータ。そしてその 2 つは下に寄せる」）+「確認も挟みたいよね」: 一括で状態を書き換える操作は差分の確認を挟む。読み取れる好み = 入口はサイドメニューに集約して本線の入口を上・付随物を下へ寄せる / 取り消せない操作は実行前に何が変わるかを見せる（`ui-flow.md` と `ui-parts.md` に反映済み）。同日の続き: 「説明文が長い。こちらの通りじゃない。コピーできるプロンプトをはれ」= 外部ツールへの手順は文章やリンクでなく**コピーできる本文**を画面に置く / 「取り込んだ確認ページももう少し洗練して」= 件数は見出しの右端、同種の表は 1 つに統合、説明文は落とす / 「取り込んだ後の画面不要。メインに戻る」= 実行前に見せ終わった内容を実行後にもう一度見せない（完了画面を置かず元の画面へ戻す）/ 「プロンプトにスキルのリンクなくね…」= 外部へ渡すプロンプトは自己完結させたうえで出典の URL も入れる / 「jsonのプレースホルダはformatしておいて」「キーはそれぞれ改行しないと」= 例示の JSON は 1 キー 1 行に整形して置く / 「skill 側にその指示を書いて次のスキルを実行してくださいでいいんじゃないの」= 外部へ渡すプロンプトに仕様を写さず、指示はスキル側に置いてポインタだけ渡す / 「変更なしの分は不要」= 差分表示に変わらない行を出さない / 「ペーストする時キーボード出すのだるいからワンボタンでペーストしたい」= 貼り付けはクリップボード読み取りの 1 ボタンで済ませる / 「AIに渡すプロンプトのように、同じスタイルでテキストエリアに、AIが出力したJSONとペーストボタン作って」「確認は自分でボタン押す」= 渡すものと受け取るものを同じ枠（ヘッダ + 操作ボタン）で対にし、入力の補助と次へ進む操作は分ける / 「データの取り込みの冒頭文不要」= 画面の冒頭に手順の説明文を置かない（3 回目。ui-design.md の「手順の説明文で UI を補わない」の再確認）

（以下は昇格済み — 2026-09-10 の棚卸し 28 回目で「メンバーの固定は複数選択ピッカーに」の事例を `ui-parts.md`（複数入れるピッカーは選んでも閉じない・メンバー 5 枠は 5 列のタイル・空き枠は 1 つのおまかせ）と `ui-flow.md`（枠はどれもピッカーの入口）へ統合。棚卸し 27 回目では `ui-parts.md`（結果詳細の並び・内訳は既定で畳む・主数値の行の脚注番号と操作の位置・脚注は初期表示では画面に出さない・同じ見た目の表を続けて置かない・数値は既存のピッカーで選ばせる・結論に使わない情報は出さない）と `ui-verification.md`（全画面に効く扱いは対象の全画面で実測する）へ統合。2026-09-09 ぶんは棚卸し 26 回目で統合済み）
