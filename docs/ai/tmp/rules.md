# 未整理のルール候補（induction 記録先）

housekeep 時に必ず昇格・整理される。書き方は induction スキルを参照。

## Rules

（なし — 2026-09-10 の棚卸し 29 回目で `ui-design.md` へ 2 件昇格: **外部のツール・AI に渡す指示を画面に書き写さずスキルを指すポインタだけ置く**、**モバイルでキーボードを出させる入力にはワンボタンの代替を用意する**（`clipboard.readText()` が解決も失敗もしない環境の保険つき）。棚卸し 30 回目で `ui-design.md` へ 1 件: **スワイプで隠れた操作を出す UI を使わない**（初見で操作が分からない）。26 回目ぶんは `storage-compat.md` と `ui-parts.md` へ統合済み）

## Cases

- 2026-09-10「スラッシュ区切りなんかやだな。片方だけいいえの可能性あるし、別の質問としたら？」: 1 行に確認が 2 つあるとき「A？ / B？」と連結していたのを別の問いに分けた（`cautions: string[]`。行は自分の問い全部に「はい」で取り込む）。読み取れる好み = **独立に答えうる問いを 1 つの問いに束ねない**（`ui-parts.md` に反映済み）

- 2026-09-10「skill.md 修正。これに一本化して。references とか不要になる」: ユーザーが SKILL.md の本文を書き下して渡し、`references/owned-members.md` を廃止した（スキルは 1 ファイルで完結。URL で渡す前提なら追い取得が要らない形が確実）。同時に封筒から `kind` が消えたので、取り込み側は `kind` を任意扱いにして別種別が明示されたときだけ断るようにした。読み取れる好み = **URL で渡すスキルは 1 ファイルで完結させ、参照ファイルに分割しない**（1 回目。同種の 2 回目が来たら規則にする）。あわせて、スキル側の書き方の指示: 一覧にホロメン名・カード名が出ないのは正常だと明記して「止まる理由」を潰す / 画像認識 + web 検索 + 複数情報の照合による一意特定は「推測」ではないと定義する / 禁止事項を「処理全体を止めてはいけない理由」の列挙で書く

（なし — 2026-09-10 の棚卸し 30 回目で「スクショからの取り込み」の事例をすべて `ui-parts.md`（判断は実行前の はい / いいえ 質問・件数だけ出すエラー・3 区分の並び・開花アイコン 1 つ・冒頭の説明文なし・渡すものと受け取るものを同じ枠で対に）と `ui-flow.md`（入口はサイドメニュー最上段・3 区分）と `structure-import` スキル（並びは左上から右下・ブレの吸収・カード名は任意）へ統合。**途中で試して撤去した案**（行の左スワイプ削除・左右スワイプの 2 択）も `ui-design.md` の「スワイプで隠れた操作を出す UI を使わない」として残した。29 回目では「結果詳細のカードをタップでカード詳細」を `ui-parts.md` へ、28 回目では「メンバーの固定は複数選択ピッカーに」を `ui-parts.md` / `ui-flow.md` へ統合済み）
