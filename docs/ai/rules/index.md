# エージェント向けルール索引

エージェント向けの恒久ルールの一覧。ルール本体はこのディレクトリ以下に1トピック1ファイルで置き、追加・削除時はこの表を更新する。

- 特定のパス配下でのみ適用されるルールは、ここではなく `.claude/rules/`(必ず `paths:` glob つき)に置く。
- 未整理・検証前のルール候補は [../tmp/rules.md](../tmp/rules.md)(induction スキルの記録先)にあり、housekeep 時にここか `.claude/rules/` へ昇格される。

| ルール     | 概要 |
| :--------- | :--- |
| (まだない) |      |
