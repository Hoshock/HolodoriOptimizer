# 表示ユニットスコアの逆解析

ユニット編成画面に表示される「スコアボーナス」と「ユニットスコア」の現在の知識をまとめる。実測値そのものは [repro/display-score-20260912.md](./repro/display-score-20260912.md)、証拠の扱いは [evidence-policy.md](./evidence-policy.md) を正典とする。

`src/engine/displayScore.ts` は現在の試算実装であり、反証済みの近似が残る。コードが存在することをゲーム仕様の証拠にしない。

## 確定・強く支持されること

### ユニットスコアの外側

```txt
unitScore = ceil(totalPower × (1 + scoreBonus / 100) × 2.03734)
```

多数の実機報告で1点単位まで一致する。新しい観測もこの式で cross-check する。ただし式が一致しても、編成・Lv・カードの読み違いまでは検出できない。

### 5カテゴリ

現在の実機では次の5カテゴリを観測できる。

1. 衣装（Costume / Leader）
2. アクティブ（Active）
3. ホロメンボード（Board / SkillTree）
4. パッシブ（Passive）
5. SP（Special）

0のカテゴリは表示上省略されることがある。【外部情報】サーバー応答にも Active / Leader / Passive / SkillTree / Special の独立カテゴリがあるとされる。現在実装のタイムライン差分配賦をゲーム内部式とみなさない。

### 黄ボード

同一編成・同一赤状態の「曲なし Y=0」と「Gamers Y=10%」の直接比較では、Boardだけが `40.7 → 64.8`（+24.1）となった。他4カテゴリは `Costume 14.1 / Active 77.7 / Passive 1.9 / SP 47.0` で同じ。

表示値でも、

```txt
0.1 × (100 + 14.1 + 77.7 + 1.9 + 47.0) = 24.07
```

となる。したがって現在の最も強いモデルは、量子化前rawを用いる

```txt
YellowBoardIncrement
= Y × (100 + Costume + Active + Passive + Special)
```

である。Costumeを除く旧4カテゴリ式はこの直接対照に合わない。

実装（`songBoardRaw`）は2026-09-12にこの5カテゴリ式へ更新した。`DisplayScoreBreakdown` も `costume` を含む5カテゴリで、合計は5欄の表示値の和。Costume欄の現在値は「衣装スコアサポートを静的に (1 + X/100) 倍した増分」という既知の近似で、実機（13〜17pt台）より大きい。Costumeの一般式は未解明のまま。

### 赤のパラメータ効果と赤スコアサポートは分ける

赤の P/T/S 固定値・割合を変えて総合力が変化しても、スコアカテゴリが変わらなかった対照がある。赤のパラメータ計算を、赤「全員のスコアサポート効果」の表示換算へ混ぜない。

## 強い推定

### Active

200秒の共通仮想タイムラインを使う現在モデルが既存Goldenに非常によく一致する。

- 発動候補: `k × interval <= s < k × interval + duration`、`k >= 1`
- 基礎発動確率: 高55% / 中46% / 低37%【外部情報を含む】
- 同時候補時は確率和で正規化する現在モデル
- Activeの0.1%単位化は切り上げで既存観測と一致

発動確率の実数値自体はゲーム内表示から直接確認できない。

**K5 / K6 の不一致は入力誤りだった（2026-09-13）**: 恒常そら / アキ / スバル / フレア / ぼたん 0凸を含む K5 / K6 で評価器が 67.9 → 59.6（K6 71.4）と実機 63.3 / 73.7 を外していたのは、0凸アクティブの入力（仮定倍率、次いで抽出マスターの level 1 を 0凸に当てたもの）が誤っていたため。カード詳細画面で再確認した実機値（そら 100、ぼたん 60→125。[card-data-provenance.md](./card-data-provenance.md)）に直すと、式を変えずに raw 63.24 / 73.69 → 表示 63.3 / 73.7 で一致する（`displayScore.test.ts`「K5 / K6 のアクティブ欄」）。ライフ・コンボ・タイプ人数の条件つきアクティブを production どおり扱って一致したので、K5 / K6 を根拠にした「条件つきアクティブの表示評価方式が未解明」という主張は撤回する。

### パッシブ欄（既知の近似）

production のパッシブ欄（供給側の発動確率で重みづけした支援行列）は、恒常マリン 1凸のパッシブ「3期生が2人以上で3期生2人のスコアサポート効果」を 2026-09-12 に抽出マスター level 1 = 9%（旧仮定倍率 12 ÷ 1.1 ≈ 10.9）へ訂正した後、恒常ぺこらとの 2 人で条件が成立する Golden 9.8〜9.11 で実機 2.4〜3.5 に対し 1.7〜2.1（不足 0.7〜1.8）になった。正しい入力ほど不足が大きく、モデルの既知の近似として上限だけ更新した（`displayScore.test.ts`。Golden は不変）。

### SP

現在モデルは多くのケースで近いが、発動率UPを含むケースなどに残差がある。Activeほど確度は高くない。

## 赤「全員のスコアサポート効果」

### 観測から安全に言えること

- 表示への効きは編成依存。赤+24の報告差は現在 `+16.4〜+20.9pt` まで広がっている。
- 赤+24の曲比較ではActive / SPが変わらず、Boardを中心にCostume / Passiveも動く例がある。
- R-061（+3）だけを開いた対照ではPower不変、Bonus `178.7 → 181.4`（+2.7）。
- 同じ白上フブキのホロメンボード状態でも、水着フブキ0凸と恒常フブキ1凸で赤+24の差が異なる報告がある。これは「青ボード量だけ」では説明できないが、カード変更に伴いActive / Passive等も同時に変わるため原因は特定できない。
- 水着ミオ + 恒常マリンでは、青補正なし候補秒149/200に対して赤+24の実測差が+20.9で、旧候補秒率式の17.88を大きく外す。

### 棄却済み

一般式として使わない。

- `固定係数 × X`（0.88、0.85等）
- `X × 基準候補秒 / 200`
- `X × 表示Active / 90`
- Costume表示値だけで決める単純倍率
- 青込み候補秒・発動確率込み占有率だけで決める単純式

現行 `redScoreSupportDisplayGain` は旧候補秒率式を残した**既知の近似**で、一般式として反証済み。実装を根拠にGoldenや正典を書き換えない。

### 現在最有力の一般則候補（強い推定。確定ではない）

```txt
ΔRed(X) = X / 100 × E_blue
```

`E_blue` は production のアクティブ欄と同じ評価器（200秒の共通仮想タイムライン、条件解決済みスコアUP、同時候補は確率和 `Σ up × p / max(1, Σ p)` で正規化）に、青ボードを次のように入れたアクティブ期待値。

- 発動確率: `p_i = min(1, p0_i × (1 + 発動率UP_i / 100))`（**乗算型**）
- 発動周期: `interval_i / (1 + 発動頻度UP_i / 100)`
- 候補窓: `k × interval_i' <= s < k × interval_i' + duration_i`（`k >= 1`、200秒）

実機13対照（[repro](./repro/display-score-20260912.md) の赤+24 × 9、R-061 +3、旧R-002 +10 × 3）に対する raw 予測は RMSE ≈ 0.09 pt、最大絶対誤差 ≈ 0.13 pt（2026-09-12、各対照はその観測時点の青実効値を使用）。旧候補秒率式は同じ13対照で RMSE ≈ 1.0 pt（+24の9件だけなら ≈ 1.2 pt、水着ミオ+恒常マリンで 3 pt 外す）で、大幅に改善する。発動率UPを production の青と同じ加算型 `p0 + r` にすると RMSE ≈ 0.85 pt になり、乗算型が明確に良い。

解析コードは `src/engine/displayScoreExperimental.ts`、回帰評価は `displayScoreExperimental.test.ts`（仕様のGoldenではなく仮説の回帰ガード: RMSE < 0.15 / 最大 0.25）。

**production には採用しない。** 総増分は説明できても、実機ではその増分が Costume / Board / Passive のカテゴリへ分かれて動き（赤+24で Costume +0.5 / Board +20.4 / Passive 0 など）、その配賦式が未解明のため。総量だけ合わせて全部 Board に入れると5カテゴリ表示が誤り、黄が Costume / Passive を基底に使うぶん黄込みの結果にも波及する。また乗算型が赤の内部基準量として強いことは、production の青 Board 換算を乗算型へ変える根拠ではない（青の一般仕様として `p0 × (1 + r)` が確定したわけではなく、raw の server permil も未取得）。

### カテゴリ配賦（Costume / Board / Passive）は未解明 — 既存評価器上の逐次 marginal は全順序で反証

総増分 `X/100 × E_blue` を、production と同じ評価器 F の上で「スキルツリー赤（S）・リーダー衣装（L）・パッシブ支援（P）」の 3 供給源を順に足したときの限界寄与として配賦する仮説を、2026-09-12 に 6 順序 × 供給側確率 2 種（`p0` / 青乗算型）× 量子化 2 種（round / ceil）の 24 組で評価した（`src/engine/displayScoreAttributionExperimental.ts` / `.test.ts`、production 未採用）。コーパスは赤だけを変えた直接比較 15 行（水着フワワリーダー +24 × 9、R-061 +3、衣装なし R-002 +10 × 3、水着ミオリーダー +24 の黄 0 / 10%）で、各行に赤変更前後の 5 欄の絶対値を持つ。黄の交差項は raw 5 欄 → `songBoardRaw` → 量子化の順で分離し、量子化は全ケース同一とした。

**結果: どの組も 衣装 ΔC ≤ 0.25 / パッシブ ΔP ≤ 0.25 / ボード ΔB ≤ 0.5 pt を同時に満たさない。**

| 順序（先 → 後）                 | ΔC RMSE / 最大   | ΔP RMSE / 最大   | ΔB RMSE / 最大   | 主な破綻                                                                                           |
| :------------------------------ | :--------------- | :--------------- | :--------------- | :------------------------------------------------------------------------------------------------- |
| S → P → L, S → L → P            | 3.65〜3.70 / 5.3 | 0.19〜0.31 / 0.5 | 0.73〜0.75 / 1.2 | 衣装欄が赤で ×(1+X/100) され、+24 で ΔC ≈ +5.2（実機 −0.1〜+0.5）                                  |
| P → S → L                       | 3.68〜3.70 / 5.3 | 0.20 / 0.5       | 0.89〜0.98 / 1.5 | 同上。パッシブを先に取るとボードも悪化                                                             |
| L → S → P, L → P → S, P → L → S | 0.30 / 0.5       | 0.20〜0.31 / 0.5 | 4.23〜4.47 / 6.2 | 衣装は動かないが、赤の総増分が衣装で 1.25 倍されボードが +24 で ≈ +5 過大、R-061 +3 でも +0.7 過大 |

round と ceil の差は ΔP で最大 0.1、ΔB で最大 0.1 程度で、破綻の大きさ（数 pt）に比べ無視できる。供給側確率を `p0` から青乗算型へ変えても同様（差 ≤ 0.1）。2026-09-12 に恒常ころね 3凸のパッシブ「ハッピータイプ2人のスコアサポート効果」を仮定倍率 10 → 抽出マスター level 1 = 8% に訂正しても（恒常フブキ1 / 恒常ころね3 の ΔP モデル値 0.3 → 0.2、ΔB 21.2 → 21.1）、この反証は変わらない。9 編成の主要アクティブ入力は訂正の対象外（水着みこ1 / ミオ1 / ころね2、恒常マリン1 / フブキ1 / ころね3 は Lv2、水着フワワ0 / フブキ0 / ノエル0 は実機 variant）なので、衣装欄・ボード欄の配賦問題は入力の推定ミスでは説明できない。

**衣装欄は一様倍率ではない（構造的な下限による棄却）。** 衣装スキル「ピュア 2 人以上で全員のスコアサポート 25%」を、どの配賦法であれ全員のスコアに掛かる一様な ×1.25 として表すなら、衣装欄の値はどの順序でも `0.25 × E_base`（青・赤なしのアクティブ期待値。L を先頭に取った限界寄与がちょうどこの値）以上になる。水着フワワリーダー 9 編成でこの下限は 17.7〜19.9 pt だが、実機の衣装欄は 13.4〜17.4 pt で全編成が下限を下回る（比 0.72〜0.97）。したがって衣装欄の算出は「一様倍率 × 何らかの配賦」の形ではなく、配賦順序を変えても救えない。また衣装欄は同じ X でもメンバーで大きく変わり（水着ミオ1 + 水着ころね2 で 14.1、水着ミオ1 + 恒常マリン1 で 17.4）、X 26 → 50 で −0.1 になった編成もある。22 個の衣装欄観測に対する単一特徴量の探索では、どの候補も編成間の差を 22% 以内に収められなかった。

**パッシブ欄の増分は既存の支援モデルと大小関係が逆。** 衣装なしの対照で、実機は R-002 +10 でパッシブ +0.4 / +0.5 / +0.3、水着ミオ +24 で +0.2 だが、既存の支援行列（供給側 `p0`）を S の後に取った限界寄与は +0.2 / +0.1 / +0.1 と +0.5 で、+10 の方が +24 より大きい実機の関係を再現しない。パッシブのスコアサポートが赤とどう合成されるかは未解明のまま。

**青の provenance 衝突（総量モデルの感度）。** 水着フワワの青は 2026-09-09 の実機報告で発動率 +45% / 頻度 0 だが、2026-09-12 の構造化データ（アカウント状態）にはフワワの青マスがなく、みこも 36（09-09）と 42（構造化データ）で食い違う。総量モデルの水着フワワリーダー 10 対照は、0 / 0 & みこ 42 で RMSE 0.086 / 最大 0.128、45 / 0 & 42 で 0.201 / 0.392（系統的に +0.1〜+0.4）。モデルの当てはまりを provenance の裁定に使わず、カード画面の再確認待ちとする。exploratory 行（水着みこ → 恒常みこ、Lv「約 20」）は総量モデルが +17.4 を返し実機 +16.4 と約 1.0 ずれる。入力条件（Lv・スキル Lv）が未確認なので fit に入れず、不一致として記録する。

公開情報の探索（`LiveDeckEvaluationScoreUpPermilUp`、`by_live_leader_skill`、`score_up_permil_up_by_skill_tree` 等）では、これらのキーの算出コードや式は見つからなかった。

**次の実験。** 青なし・パッシブ支援ありの clean 対照（K6）は 2026-09-13 に実施済み。現在の優先は下の「次の実機観測（1 つ）」（水着フブキ 0凸 を含む編成）。赤側の 1 要因実験（水着ミオ1 / 恒常マリン1 で FUWAMOCO のまま赤マス 1 つだけを開閉し dC/dX の符号を見る）はその後。

### 現在は原因仮説を固定しない

以前は「水着フブキ0凸を含むケースだけ低い」ことから、水着フブキのパッシブ・スコアサポートとの相互作用を本命仮説としていた。しかしカード仕様の参照ミスが発生し、さらに水着みこ→恒常みこへ変更した新観測では、同時にActive / Passive / SP / Costume成立など複数条件が変化して赤+24差が+16.4まで下がった。

このため、**水着フブキのパッシブを原因とする仮説は正典から撤回する**。数値相関は観測として残すが、対照実験が成立するまで特定カード・特定スキルを原因扱いしない。

次の実験は、カードID・開花・Lv・リーダー/メンバー役割・衣装条件・各スキルprovenanceを事前に監査し、変更要因を1つに近づけられるものだけ採用する。

## リーダー衣装のスコアサポート: 総量とカテゴリ配賦（2026-09-12 典獄クロニー 60%）

材料は [repro](./repro/display-score-20260912.md) の「Leader-only matched pairs」K1〜K6: 同じメンバー 5 人・赤 0・黄 0・曲指定なしで、リーダーだけを 恒常みこ 0凸（支援 0%）→ 典獄クロニー 0凸（衣装「全員のスコアサポート効果60%」、条件なし）に替えた 5 欄の差。K5（青なし・パッシブ支援なし）と K6（青なし・恒常マリン 1凸のパッシブ支援 9% あり、2026-09-13）は negative control。解析コードは `src/engine/displayScoreLeaderSupportExperimental.ts` と `displayScoreSourceAttributionExperimental.ts`（いずれも production 未採用）。K1〜K4 のアクティブ欄は評価器の `E_base` と 0.1 以内で一致し、クロニー側 4 件はユニットスコア式に 1 点単位で一致する。

### 総量: `Δ衣装 + Δボード + Δパッシブ = S/100 × E_blue(乗算)`（強い推定）

S = 60 で、赤の総量モデル（上の `X/100 × E_blue`）と**同じ評価器・同じ E_blue** を使う。

| ケース（A / B 枠）                                 | 実測 Δ衣装 / Δボード / Δパッシブ | 実測合計 | 0.60 × E_base                         | 0.60 × E_blue(加算) | 0.60 × E_blue(乗算) |
| :------------------------------------------------- | :------------------------------- | -------: | :------------------------------------ | ------------------: | ------------------: |
| K1 水着ミオ1 / 水着ころね2                         | 38.6 / 12.6 / 1.1                |     52.3 | 46.6                                  |                54.9 |                52.3 |
| K2 水着ミオ1 / 恒常マリン1                         | 46.2 / 6.3 / 0                   |     52.5 | 43.2                                  |                54.6 |                52.4 |
| K3 水着フブキ0 / 恒常マリン1                       | 38.4 / 7.4 / 0.4                 |     46.2 | 42.5                                  |                49.3 |                46.2 |
| K4 水着フブキ0 / 水着ころね2                       | 37.0 / 9.5 / 1.4                 |     47.9 | 43.0                                  |                50.7 |                47.7 |
| K5 clean（恒常そら/アキ/スバル/フレア/ぼたん 0凸） | 37.9 / 0 / 0                     |     37.9 | （表示アクティブ 63.3 × 0.6 = 37.98） |                同左 |                同左 |
| K6 青なし + パッシブ 2.9（ぼたん → 恒常マリン1）   | 44.3 / 0 / 0                     |     44.3 | （表示アクティブ 73.7 × 0.6 = 44.22） |                同左 |                同左 |

K1〜K4 で乗算型は RMSE 0.11 / 最大 0.18、加算型は RMSE 2.7（+2.1〜+3.1 過大）、`E_base` は RMSE 6.2（−3.7〜−9.3 不足）。したがって、**リーダー衣装のスコアサポートと赤のスコアサポートは「総増分」としては同じ基礎量 `E_blue(乗算)` に比例する**。過去の同じ 5 人での 水着フワワ 25% + 赤 26%（黄 10% を表示値で除く）とクロニー 60% + 赤 0 の合計差も、4 組とも `(60 − 25 − 26)/100 × E_blue` に ±0.2 で一致する（K1 7.67 vs 7.84、K2 7.81 vs 7.86、K3 6.89 vs 6.94、K4 7.14 vs 7.16）。

K5（青 0・パッシブ支援なし）は clean control: ボード・パッシブは動かず、衣装欄 37.9 = 0.60 × 表示アクティブ 63.3（37.98）。表示アクティブは切り上げなので raw は (63.2, 63.3]、`0.6 × raw` は (37.92, 37.98] で、衣装欄の 37.9 は切り上げでは出ない（切り捨てなら区間全体、四捨五入なら raw < 63.25 のとき）。これだけで量子化規則は決めない。K6 の 44.3 は逆に切り上げでしか出ないので、単一の量子化規則はここでは決めない。**K5 / K6 の 5 枚の 0凸アクティブは 2026-09-13 にカード詳細画面で再確認した実機値（`observed-variant`）**: そら 100 / アキ 50（ライフ 600 以上で 95）/ スバル 95 / フレア 50（40 コンボ以上で 100）/ ぼたん 60（ハッピー 2 人以上で 125。この 5 人では不成立）。この入力で production の評価器は K5 raw 63.24（表示 63.3）/ K6 raw 73.69（表示 73.7）を式の変更なしに再現し、`0.60 × raw` は K5 37.94（衣装欄 37.9、+0.04）/ K6 44.22（44.3、−0.08）で 0.1 以内。K5 / K6 は表示アクティブの代理を使わず、評価器の値から clean control が成立する。

### カテゴリ配賦は総量と別問題（衣装 = 0.60 × E_blue ではない）

K1〜K4 では同じ 60% が 衣装 38.6 / ボード 12.6 / パッシブ 1.1（K1）〜 衣装 46.2 / ボード 6.3 / 0（K2）へ分かれ、K5 / K6 では全量が衣装。**青がない K5 / K6 では、パッシブのスコアサポートの有無（K6 はパッシブ欄 2.9）によらずリーダー支援の増分は全量衣装欄で、パッシブ支援単独ではカテゴリ再配賦を起こさない。** 現在のコーパスでは、ボードへのリーダー由来増分には青が必要で、パッシブへのリーダー由来増分は青あり・パッシブ成分ありの K1 / K3 / K4 だけで観測されている（必要十分条件とは言わない。K2 は青あり・パッシブ欄 0 で Δパッシブ 0）。衣装欄を `0.60 × E` にした値の観測 `E_C = Δ衣装 / 0.6` は K1 64.3 / K2 77.0 / K3 64.0 / K4 61.7 / K5 63.2 で、K2 だけ `E_base`（72.0）より大きく、他は下回る。

衣装欄の候補（自由係数なし。値は 0.60 × E の K1 / K2 / K3 / K4 の誤差 pt）:

| 衣装候補の E                                                      |    K1 |   K2 |   K3 |    K4 | RMSE | 判定                      |
| :---------------------------------------------------------------- | ----: | ---: | ---: | ----: | ---: | :------------------------ |
| `E_base`（一様倍率）                                              |  +8.0 | −3.0 | +4.1 |  +6.0 |  5.7 | 棄却（両符号）            |
| `E_blue(乗算)`（総量そのもの）                                    | +13.7 | +6.2 | +7.8 | +10.7 | 10.0 | 棄却（K1〜K4 が直接否定） |
| 主仮説: 編成条件を未解決にした基準 up × 基準タイムライン × p0     |  −1.5 | −7.0 | +0.4 |  −3.0 |  3.9 | 棄却（K2）                |
| C*: 編成条件を未解決にした基準 up × 青の頻度込みタイムライン × p0 |  +1.5 | −0.3 | +1.4 |  −1.5 |  1.3 | 最良だが式ではない        |

「編成条件を未解決」は、タイプ人数・所属人数の条件つきスコア UP（水着フワワ 120 → 60、水着ころね 120 → 60）を基準値で評価し、ライフ・コンボ条件は production と同じく成立扱いにするもの（前回の swap 方向 9/10 の手掛かりの再検証）。主仮説は K2 を 7 pt 外す: K2 の観測 `E_C = 77.0` は `E_base` 72.0 より大きく、水着ミオ 1凸の発動頻度 8% で恒常マリン 1凸との同一窓（周期 23 秒 / 効果 8 秒）がずれる効果（基準 65.4 → 頻度込み 76.6）を入れないと届かない。C* はその頻度を入れたものだが、K1 / K3 を +1.4〜1.5 過大、K4 を −1.5 過小にし、誤差の符号はパッシブ支援の有無（K2 だけなし）と対応しない。青の発動率 UP（乗算型 p）まで衣装欄に入れると K1 で +4.4 過大になる。

残差 `R = 0.60 × E_blue(乗算) − C*` は Δボード + Δパッシブ を ±1.5 で追う（総量が合う以上、衣装の誤差の鏡像）。パッシブ欄の増分は、既存のパッシブ支援 marginal（供給側 `p0`、青乗算型）を `S/100` 倍した値の 1.6〜2.0 倍（K1 1.1 vs 0.58、K3 0.4 vs 0.23、K4 1.4 vs 0.82）で、自由係数なしには分離できない。ボード = 総量 − C* − パッシブ候補 の conservation は ±2 pt（K4 で +1.9）。

**リーダーなし（恒常みこ、支援 0）のボード欄**にも新しい観測がある: パッシブ支援のない K2 ではボード欄 15.3 が `E_blue(乗算) − E_base` = 15.28 に一致し（production の加算型は 18.9）、パッシブ支援のある K1 / K3 / K4 では同じ差より 1.6 / 0.5 / 1.8 高い。青の発動率 UP のボード換算（未解明）に対し、乗算型を支持する 1 点と、パッシブ支援との交差項の存在を示す 3 点として残す。production の青は変えない（既存 Golden 20 ケース側の再評価が先）。

**過去の水着フワワ 25% との接続。** 総量は上のとおり整合するが、衣装欄は C*（S=25）より 7〜18% 低い（同じ 5 人でクロニー 60% の `E_C` に対し 0.82〜0.93）。リーダーと同じホロメン（水着フワワ 0凸メンバー）を支援対象から外す構造仮説では 9 編成 RMSE 0.8 / 偏り +0.5（恒常フブキ1 / 恒常ころね3 で +1.6）で、前回の候補族の最良（1.00）より良いが式ではない。1 データセットの手掛かりとして記録し、採用しない。

**判別実験の結果（K6、2026-09-13）。** 上の予測どおり 衣装 = 0.60 × 表示アクティブ（44.22 vs 44.3）、Δボード = 0。ただし「Δパッシブ ≈ 0.6 × パッシブ marginal（≈ 0.6）」は外れて Δパッシブ = 0 で、「パッシブ支援との相互作用が衣装欄を減らす」側も外れた。リーダー支援とパッシブ支援の間に相互作用はなく、配賦異常の起点は青に絞られた。

### 支援 3 種の合成と総量（2026-09-13、K6 と赤 / 衣装コーパス 11 行）

`src/engine/displayScoreSourceAttributionExperimental.ts` / `.test.ts`（production 未採用）で、秒 × 発動候補単位に source（青の頻度 Q / 発動率 R / パッシブ支援 P / リーダー衣装 L / 赤 X）を持たせて総量と配賦を調べた。

- **支援は加算合成（強い推定）。** 候補 i の値を `up_i × p_i × (1 + L + X + P_i) / max(1, Σp)` とすると、K6 の総増分 44.3 は 0.6 × 73.7 = 44.2、K1〜K4 は既存どおり `S × E_blue(乗算)`（RMSE 0.11）。乗算合成 `(1 + L)(1 + X)(1 + P_i)` は K6 で +1.7、水着フワワリーダー 10 行（支援 48〜75%）で +5〜+13 過大で棄却。
- **パッシブのスコアサポートは対象に常時掛かる静的な支援（総量としては強い推定）。** production は供給側も発動候補の秒だけ `S_ji × p_j` を足す（gated）が、リーダーなしの ボード + パッシブ 欄の和は `(E_blue − E_base) + 静的パッシブ` で K1 −0.16 / K2 −0.02 / K3 −0.21 / K4 −0.12 / K6 −0.05、gated は 0.6〜1.9 不足。赤 / 衣装コーパス 11 行（2026-09-12 の青）の 4 欄合計も加算合成 × 静的で赤の前後 22 点すべて −0.1〜−0.4（4 欄切り上げの偏り程度）、gated は −1.4〜−2.2。2026-09-08 の青を仮定した 4 行（水着ミオの青を 0 / 0 と仮定）は静的でも −1.5〜−2.2 で、青の入力が未確認なので fit に入れない。パッシブ支援の対象は production と同じ「count 人」限定（K6: マリン → フレア・マリン、K1: 水着ころね → 水着フワワ・水着ころね）で、全員化はしていない。
- **パッシブ欄そのものは矛盾したまま。** K1 / K3 / K4 の実機（1.0 / 0.5 / 1.5）は静的値（2.49 / 0.82 / 3.25）の 40〜60% で gated（1.35 / 0.43 / 1.82）に近く、K6（2.9）は静的値（2.85）に一致する。差分はボード欄に出る（K1 / K3 / K4 のリーダーなしボード欄が `E_blue − E_base` より 1.6 / 0.5 / 1.8 高い）。青の有無で列の分割が変わるが規則は未解明（K6 の静的値は実機 0凸値で 2.9、gated は 1.0）。

### 秒 × 候補単位の配賦ルール（負の結果）

同じハーネスで、候補 i の各秒の支援ぶんを「自分の source に残す / ボードへ回す」割合だけを変える 5 ルールを比べた（自由係数なし。総量は不変）。

| ルール                                                     | 衣装へ残す割合   | K1〜K4 Δ衣装 誤差（pt）     | Δ衣装 RMSE | Δボード RMSE | Δパッシブ RMSE | フワワ 9 編成 衣装 RMSE |
| :--------------------------------------------------------- | :--------------- | :-------------------------- | ---------: | -----------: | -------------: | ----------------------: |
| lastApplied（= S × E_blue）                                | 1                | +13.7 / +6.2 / +7.8 / +10.7 |       10.0 |         10.0 |            0.9 |                     ≥ 5 |
| interactionToBoard（= S × E_base、青との相互作用はボード） | 基準タイムライン | +8.0 / −3.0 / +4.1 / +6.0   |        5.7 |          5.7 |            0.9 |                     ≥ 4 |
| baseWindow（頻度 UP で増えた秒はボード）                   | 基準窓の秒だけ   | −3.6 / −12.9 / −2.3 / +1.2  |        6.8 |          6.8 |            0.9 |                     ≥ 2 |
| baseWindowP0（上に加え発動率 UP ぶんもボード）             | 基準窓 × p0/p    | −11.6 / −17.7 / −7.3 / −7.1 |       11.6 |         11.6 |            0.9 |                     ≥ 3 |
| p0Only（発動率 UP で増えた確率ぶんだけボード）             | p0/p             | +1.2 / −3.6 / +0.2 / 0.0    |        1.9 |          1.9 |            0.9 |                   ≥ 1.5 |

青を頻度 / 発動率の 2 source に分けた対称配賦（Shapley、順序 6 通りの平均）も K1 +10.9 / K2 +1.4 / K3 +5.7 / K4 +8.3 で棄却。加算合成では L × P の相互作用がないため、どのルールもリーダー由来の Δパッシブ を 0 と予測し、K1 / K3 / K4 の +1.1 / +0.4 / +1.4 を出せない。最良の p0Only は K1 / K3 / K4 の衣装を 1.2 以内で追うが、パッシブ支援のない K2（水着ミオ 1凸の頻度 8% で恒常マリン 1凸と同一窓がずれる編成）を −3.6 外し、K2 を合わせる C*（頻度込み窓 × p0 × 編成条件未解決 up）は逆に K1 / K3 / K4 を 1.4〜1.5 外す。**衣装 / ボード / パッシブ の配賦は青の頻度・発動率・パッシブ支援と 3 者で絡み、候補単位の単純な帰属では説明できない**（成功条件の 0.1〜0.2 pt には届かない）。production は変えない。

**次の実機観測（1 つ）。** クロニー / 恒常みこリーダーで、メンバー 恒常そら0 / 恒常アキ0 / 恒常スバル0 / 恒常フレア0 / **水着フブキ0**（青は水着フブキの発動率 15% だけ。水着フブキのパッシブ「キュートタイプ2人のスコアサポート8%」はそら・スバルが対象で、供給側と対象が分かれる）の 5 欄を読む。リーダーなしのパッシブ欄は静的なら ≈ 2.0、gated なら ≈ 0.5、青の純増分 ≈ 0.2 で、供給側だけに青があるときのパッシブ欄の縮みを K3（供給側 = 対象 = 水着フブキ）と分けて判定できる。クロニー側は総増分 0.6 × 表示アクティブ を確認する。**この観測の主目的は 2026-09-13 に移った**: パッシブの kernel が静的であること自体は K6 で決着したので、いま知りたいのは (1) 青が発動率 15% だけのときの `W_blue`（下の「source 分離型の projective weight」で唯一残っているボトルネック）と、(2) 残差が 水着フブキ 0凸 を含む行に集中する原因が `W_blue` の式なのか 水着フブキ 0凸 の入力なのか、の切り分け。この編成は 水着フブキ 0凸 が唯一の青持ちなので、両方を同じ 1 件で見られる。

### 非 native カテゴリの共通 scale（2026-09-13。現コーパスでは確定扱い）

`src/engine/displayScoreCategoryRedistributionExperimental.ts` / `.test.ts`（production 未採用）。上の 5 ルールと違い、**総量を増やさず表示カテゴリ間で再配分する**構造。総増分 `支援/100 × E_blue(乗算)` と支援 3 種の加算合成はそのまま使う。

支援の source ごとに、ゲーム上それが直接乗る欄（**native カテゴリ**）がある。

| source                       | native   | 保存される比            |
| :--------------------------- | :------- | :---------------------- |
| リーダー衣装のスコアサポート | 衣装     | ボード : パッシブ       |
| 赤（ボード）のスコアサポート | ボード   | 衣装 : パッシブ         |
| パッシブのスコアサポート     | パッシブ | 衣装 : ボード（未検証） |

**規則**: 1 つの source だけを変えた matched pair では、**native 以外の 2 欄が共通倍率 k で scale する**（= その 2 欄の比が保存される）。計算は 1 本で、source ごとに変わるのは native の選び方だけ。ケース ID や Leader / Red 固有の分岐は持たない。

**リーダーを変える（native = 衣装 → ボード : パッシブ）。** 表示は 0.1 刻みなので真値は `[d − 0.05, d + 0.05)` として区間で判定する。

| 組  | ボード B0 → B1 | パッシブ P0 → P1 | 共通部分                                        |
| :-- | :------------- | :--------------- | :---------------------------------------------- |
| K1  | 11.1 → 23.7    | 1.0 → 2.1        | 2.1211〜2.1493                                  |
| K2  | 15.3 → 21.6    | 0 → 0            | 1.4039〜1.4197（1 列だけの diagnostic）         |
| K3  | 6.8 → 14.2     | 0.5 → 0.9        | 2.0657〜2.1111                                  |
| K4  | 9.7 → 19.2     | 1.5 → 2.9        | 1.9641〜1.9948                                  |
| K5  | 0 → 0          | 0 → 0            | 情報なし                                        |
| K6  | 0 → 0          | 2.9 → 2.9        | 0.9661〜1.0351（k ≈ 1 を含む negative control） |

実測 Δ衣装 を既知として残差を分ける oracle 試験では、K1〜K4 の Δボード / Δパッシブ が raw 誤差 0.134 以内（RMSE 0.075、表示 1 桁で全件 0.1 以内）。残差を全部ボードへ回す対照は K1 +1.1 / K3 +0.4 / K4 +1.4（最大 1.4）で棄却。

**赤を変える（native = ボード → 衣装 : パッシブ）。** 衣装欄・パッシブ欄がどちらも 0.0 でない 8 行すべてで共通部分が空でない。

| 行                               | 衣装 C0 → C1 | パッシブ P0 → P1 | 共通部分       |
| :------------------------------- | :----------- | :--------------- | :------------- |
| +24 水着ミオ1 / 水着ころね2      | 14.1 → 14.6  | 1.9 → 1.9        | 1.0283〜1.0427 |
| +24 水着フブキ0 / 水着ころね2    | 13.7 → 14.1  | 2.6 → 2.7        | 1.0218〜1.0366 |
| +24 水着ノエル0 / 水着ころね2    | 13.4 → 13.8  | 1.8 → 1.9        | 1.0223〜1.0375 |
| +24 恒常マリン1 / 水着ころね2    | 15.6 → 15.9  | 2.0 → 2.1        | 1.0128〜1.0257 |
| +24 水着フブキ0 / 恒常マリン1    | 14.5 → 14.9  | 0.8 → 0.8        | 1.0206〜1.0346 |
| +24 恒常フブキ1 / 水着ころね2    | 14.5 → 15.0  | 1.9 → 1.9        | 1.0275〜1.0415 |
| +24 恒常フブキ1 / 恒常ころね3    | 15.1 → 15.5  | 2.0 → 2.0        | 1.0198〜1.0332 |
| R-061 +3 水着ミオ1 / 水着ころね2 | 14.0 → 14.1  | 1.9 → 1.9        | 1.0000〜1.0143 |

残り 8 行は 1 列しか拘束しない **one-dimensional diagnostic** で、支持ケースには数えない（衣装欄 0.0 の 6 行と パッシブ欄 0.0 の 2 行。exploratory の 恒常みこ Lv 約 20 行も衣装欄 0.0 なのでここに入り、primary fit には影響しない）。

oracle（`R = Δ衣装 + Δパッシブ` を支援なし側の 衣装 : パッシブ 比で分ける）は 8 行で **RMSE 0.043 / 最大 0.059**。

| 行         | 実測 Δ衣装 / Δパッシブ | 予測              |
| :--------- | :--------------------- | :---------------- |
| 14.1 / 1.9 | +0.5 / 0.0             | +0.4406 / +0.0594 |
| 13.7 / 2.6 | +0.4 / +0.1            | +0.4202 / +0.0798 |
| 15.6 / 2.0 | +0.3 / +0.1            | +0.3545 / +0.0455 |

対照:

| ルール                                          | 8 行での判定                                                           |
| :---------------------------------------------- | :--------------------------------------------------------------------- |
| 衣装 : パッシブ 比例（native = ボード）         | 支持。区間はすべて空でない、oracle 最大 0.059                          |
| 全部を衣装へ（Δパッシブ = 0）                   | **棄却**。パッシブ欄が 2.6 → 2.7 のように動いた 3 行と両立しない       |
| 全部をパッシブへ（Δ衣装 = 0）                   | 棄却（最大 0.5）                                                       |
| native をリーダーと取り違えて ボード : パッシブ | 棄却（Δパッシブ 最大 0.69。2026-09-13 に赤へ当てて反証されたのはこれ） |

**判定。** 2 欄とも情報を持つ matched pair は リーダー 3 組 + 赤 8 行 = 11 件で、量子化込みで意味のある反例は 1 件もない。よって「native 以外の 2 欄が共通倍率で scale する」は**現コーパスでは確定扱い**とする。2026-09-13 に「赤では B : P 比例が反証 → source ごとに routing が違う」と書いたのは、赤に リーダー用の native（衣装）を当てていた誤りで、native を正しく選べば 1 つの規則で両方を通る。

ただし、赤の 8 行は 衣装欄を強く・パッシブ欄を弱くしか拘束しない（Δパッシブ が 0.0〜0.1 で、パッシブ側の区間が広い）。強く分離できているのは「native の取り違え」と「パッシブ欄は動かない」の 2 つで、**比例配分そのものを 0.01 の精度で決めているわけではない**。

### 共通 normalization（機構の候補。ゲーム仕様とは断定しない）

非 native 2 欄の比が保存されるのは、カテゴリごとの独立な raw weight `W` と 3 欄に共通の normalization `λ` があって

```txt
C = λ W_C   B = λ W_B   P = λ W_P
```

と書ける場合と整合する。1 つの source が自分の `W` だけを変えるなら残り 2 欄の比はそのままで、両方が `λ' / λ` 倍になる（`commonNormalization` で 3 通りの native とも代数的に確認済み）。`λ` は「3 欄の合計 = 総量」から決まるので自由係数ではない。アクティブ欄・SP 欄はこの normalization の対象に入れない（どの matched pair でも動かない）。黄の楽曲スコアボーナスは既知の後段のボード加算なので混ぜない。

**この構造は 1 組の matched pair からは共通 scale と同値**で、それ以上の検証には `W` の式が要る。以下は未解決。

**衣装候補族を「最終的な衣装欄」ではなく raw weight `W_C` として入れ直す（oracle-baseline stage）。** 支援なし側の表示値をそのまま raw weight にする（`W_C = 0` / `W_B` = 実測ボード / `W_P` = 実測パッシブ。このとき baseline の λ は 1）。target は baseline 合計 + 確定扱いの総増分 `S/100 × E_blue(乗算)`。**baseline を実測で与えているので完全予測ではない。**

oracle が要求する `ΔW_C` は K1 18.10 / K2 32.73 / K3 18.56 / K4 18.75、青のない K6 は 44.30（= 総増分そのもの）。青があると総増分の 0.35〜0.63 倍に縮む。既存の衣装候補族 436 個（`ups × タイムライン × 確率 × 効き方 × 正規化 × 候補人数` の格子 + C\* / p0Only / S×E_base / S×E_blue）を入れ直しても、最良は `unconditional | 青の窓 | p0 | multiplier | 候補人数で割る` の **3 欄 RMSE 2.42 / 最大 3.96** で、成功条件の 0.2 に届く候補は 1 つもない。「最終的な衣装欄」として最良だった C\*（6.20）/ p0Only（6.11）は raw weight に読み替えても改善しない。

したがって **projective な不変量（非 native 2 欄の比の保存）は支持されるが、raw weight の式は未解明**。これが次の課題。

**パッシブ欄の分割との接続。** 支援なし側で「青の純増分」と「静的パッシブ」を raw weight 候補に取ると、和は λ = 1 と整合する（`(B0 + P0) / (青の純増分 + 静的パッシブ)` = K1 1.012 / K2 1.001 / K3 1.028 / K4 1.004 / K6 1.017）。しかし欄ごとの λ は一致しない（ボード由来 1.17 / 1.00 / 1.08 / 1.23、パッシブ由来 0.40 / 0.61 / 0.46、K6 1.02）。同じ組で 1.8〜2.9 倍ずれるので、**この 2 つは `W_B` / `W_P` としては棄却**され、和だけが合う。青のない K6 だけ両方 1 に寄る。非 native 共通 scale の規則は支援なし側の表示値を gauge にするだけで `W` の中身を仮定しないので、ここに矛盾はない。

**赤側の raw weight（参考）。** native = ボード なので `ΔW_B` を足して同じ λ で normalize する形になる。oracle が要求する `ΔW_B` は、λ を 0.1 刻みの衣装欄から逆算するため区間でしか決まらない（例: R-061 は 1.60〜3.00）。最も近い単一候補 `ΔX/100 × E_base` は 10 行中 7 行で区間の中だが 3 行で外れるので式ではない。黄ありの行は後段のボード加算を含むため、黄なしの R-061（曲なし）を別に見ている。リーダー側で `W` の式が決まっていないので、ここは本命ではない。

### source 分離型の projective weight（2026-09-13。比は支持、完全予測は 0.2 に未達）

`src/engine/displayScoreProjectiveWeightExperimental.ts` / `.test.ts`（production 未採用）。上の共通 normalization を、source ごとに分離した重みとして具体化する。

```txt
(C, B, P) = T × (W_C, W_B, W_P) / (W_C + W_B + W_P)
W_C = (L/100) × H_C      リーダー衣装のスコアサポート L%
W_B = W_blue + (X/100) × H_B   青ボード由来 + 赤スコアサポート X%
W_P = H_P                パッシブのスコアサポート
```

`T` は pre-yellow の 3 欄合計（既存の「青は乗算型・パッシブは静的・支援は加算合成」）。黄の楽曲スコアボーナスは既知の後段のボード加算なので normalization に混ぜず、pre-yellow を出したあとで戻す。

**前回の候補族試験との関係。** `displayScoreCategoryRedistributionExperimental.test.ts` の oracle-baseline stage は `W_B` / `W_P` を支援なし側の表示値に固定した（baseline の λ を 1 にする gauge）うえで候補をそのまま `W_C` に入れるもので、**その gauge での失敗**を示す。`W` は全体を定数倍しても同じ表示になるので、あれは kernel の比まで否定したものではない。そこで今回は**比を先に**測る。

**1. `H_B / H_C`（source をまたぐ gauge 非依存の比）。** 同じ 5 人で「リーダーだけ替えた組（典獄クロニー 60%）」と「赤だけ替えた組（水着フワワリーダー L=25、X=26 → 50）」を突き合わせる。`B/C = W_B/W_C` なので

```txt
H_B / H_C = (L / ΔX) × [ (B/C)_after − (B/C)_before ]
```

実測は K1 1.3489 / K2 1.2798 / K3 1.2092 / K4 1.2935（pre-yellow）。kernel はメンバーと青だけで決まりリーダーに依存しないので、この突き合わせが成立する（テストで確認）。

| 候補（H_B / H_C）                         |      RMSE |      最大 |
| :---------------------------------------- | --------: | --------: |
| **E_blue(乗算) / p0Only リーダー kernel** | **0.031** | **0.049** |
| E_blue(加算) / p0Only kernel              |     0.054 |     0.078 |
| E_blue(乗算) / C\* 基礎量                 |     0.080 |         — |
| E_blue(乗算) / E_base                     |     0.161 |         — |

**p0Only リーダー kernel** は、青の頻度込みの窓で、対象自身の確率は基準 `p0`、同時候補の競合の分母だけ青の乗算型にした量（`attributeColumns(rule: "p0Only")` の衣装欄を `L` で割ったものと同じ）。

**2. リーダーの重みは `L` に比例する。** `W_C = (L/100) H_C` なら赤が違っても `C / (L × P)` は同じ値になるはず。表示 ±0.05 の区間の共通部分は K1 0.2988〜0.3059 / K3 0.6988〜0.7539 / K4 0.2088〜0.2136 で、いずれも空でない（K2 はパッシブ欄 0.0 で値が定義できない）。

**3. `B / C` は赤 `X` に対して affine。** K1 の 5 人で X = 23 / 26 / 50 の 3 点が取れる。Red 1pt あたりの傾きは 0.0550（曲なし R-061 の中）/ 0.0557（黄 10% 行の pre-yellow を使った場合）/ 0.0540（黄 10% 行の中）でほぼ一定。X=26 は独立に 2 回測れて、pre-yellow のボードが 40.70 と 40.73 で 0.03 以内に一致する（黄の逆算 `songBoardRaw` の cross-check にもなっている）。

**4. `W_blue` と `H_P`。** 典獄クロニー側（L=60・X=0）の実測から逆算すると `W_blue` は K1 24.43 / K2 19.91 / K3 14.27 / K4 19.18、`H_P` は 2.16 / 0 / 0.90 / 2.90、K6 2.89（K5 / K6 は `W_blue = 0`）。

- `H_P`: **静的**（対象に常に `S_ji`）が最良で、`p0Only × 静的` が RMSE 0.128 / 最大 0.215。**供給側の確率を掛けない gated 版**（今回追加した候補）は青のない K6 で 1.88 を返し、要求値 2.89 を 1.02 外すので棄却。production の gated（`S_ji × p0_j`）も同じ理由で棄却。
- `W_blue`: `E_blue(加算) − H_C` が K2 を 0.06、K1 を 0.71 で当てるが、K3 / K4 を +3.51 / +3.68 過大にする。青がなければどの候補も 0 になる（K5 / K6 は自動的に満たす）。従来使っていた「青の純増分 `E_blue(乗算) − E_base`」は 10 以上外すので棄却。

**5. 完全予測。** 第一候補の組（`H_C` = p0Only kernel、`H_B` = E_blue(乗算)、`W_blue` = E_blue(加算) − H_C、`H_P` = p0Only × 静的）で、2026-09-12 の青が確定している 32 点（K1〜K6 の両側 12 点 + 水着フワワ +24 の 9 行 18 点 + R-061 2 点）を予測する。

| 欄       |  RMSE |  最大 | 水着フブキ 0凸 を含む 8 点を除くと |
| :------- | ----: | ----: | ---------------------------------: |
| 衣装     | 0.647 | 2.339 |                 0.215 / 最大 0.605 |
| ボード   | 0.677 | 2.417 |                 0.208 / 最大 0.436 |
| パッシブ | 0.169 | 0.374 |                              0.102 |

**K5 / K6 は negative control として厳密に通る**: 青がないので `W_blue = 0` になり、リーダー 60% の増分は全量が衣装欄（K5 37.94 vs 37.9、K6 44.22 vs 44.3）。K6 はパッシブ支援があってもボード欄 0・パッシブ欄 2.85 vs 2.9 のままで動かない。

**残差は一様ではなく、水着フブキ 0凸（`shirakami-fubuki-02@0`、provenance は `reconstructed-observation`）を含む 8 点に集中する。** その 8 点を外すと 3 欄とも 0.21 以下まで下がり、成功条件の 0.2 にほぼ届く。ただしこれは「その 8 点を落としてよい」という意味ではなく、**`W_blue` の式か 水着フブキ 0凸 の入力のどちらかがボトルネック**という診断。逆算した `W_blue` も K3 / K4（どちらも 水着フブキ 0凸 を含む）だけが +3.5 ずれており、同じ方向を指している。

したがって現状は **比（`H_B/H_C`・`C/(L·P)`・`B/C` の affine 性）は支持されるが、`W_blue` の決定式が未解明**。`H_C` = p0Only kernel と `H_B` = E_blue(乗算) と `H_P` = 静的 は、この枠組みの中では他候補より明確に良い。

## Costumeカテゴリ

水着フワワ0凸リーダーの衣装スキル原文は、2026-09-12にユーザーが実機で次の2行を再確認した。

```txt
ピュアタイプ2人以上で全員の全パラメータ30%UP
ピュアタイプ2人以上で全員のスコアサポート25%
```

**両効果とも「ピュアタイプ2人以上」が明記される。** 旧DBには2行目の条件を無条件と推測した構造化があり、これは誤りとして訂正対象。

Costume表示値は記載25%をそのまま表示するものではなく、成立時でも編成により13〜17pt台など変化する。一般式は未解明。青のない編成（K5 / K6、典獄クロニー 60%）ではパッシブ支援の有無によらず衣装欄 = `S/100 × アクティブ欄`（37.9 = 0.60 × 63.3、44.3 = 0.60 × 73.7）で、青があるときだけそこから離れる（上の「リーダー衣装のスコアサポート」）。

### 衣装欄の絶対値: 独立評価器の候補族はすべて反証（2026-09-12）

衣装欄を「実スコア期待値の marginal」ではなく、支援 25% を材料にした独立カテゴリ評価器として算出する候補を、自由係数なしで列挙して比較した（`src/engine/displayScoreCostumeExperimental.ts` / `.test.ts`、production 未採用）。コーパスは水着フワワ 0凸リーダーの 9 編成（Gamers X=26 / FUWAMOCO X=50）と、同一編成（水着ミオ1 / 水着ころね2）の X = 23 / 26 / 50 系列（14.0 / 14.1 / 14.6）。9 編成ともアクティブ欄は評価器の `E_base` と 0.1 以内で一致するので、タイムライン入力（カード ID・開花・条件解決・発動確率）は正しい。

| 編成（A / B 枠）          | 衣装 X=26 | 衣装 X=50 | Active | E_base | E_blue(加算) | E_blue(乗算) | 線形和 | 0.25 × E_base |
| :------------------------ | --------: | --------: | -----: | -----: | -----------: | -----------: | -----: | ------------: |
| 水着ミオ1 / 水着ころね2   |      14.1 |      14.6 |   77.7 |  77.68 |        91.45 |        87.15 |  93.46 |          19.4 |
| 水着フブキ0 / 水着ころね2 |      13.7 |      14.1 |   71.7 |  71.63 |        84.47 |        79.53 |  90.00 |          17.9 |
| 水着ノエル0 / 水着ころね2 |      13.4 |      13.8 |   74.8 |  74.80 |        87.96 |        83.86 |  93.12 |          18.7 |
| 恒常マリン1 / 水着ころね2 |      15.6 |      15.9 |   78.2 |  78.15 |        89.57 |        86.28 |  96.04 |          19.5 |
| 水着ミオ1 / 恒常マリン1   |      17.4 |      17.3 |   72.1 |  72.04 |        90.94 |        87.32 |  92.58 |          18.0 |
| 水着フブキ0 / 恒常マリン1 |      14.5 |      14.9 |   70.8 |  70.78 |        82.11 |        77.07 |  89.12 |          17.7 |
| 恒常フブキ1 / 恒常マリン1 |      16.5 |      16.8 |   77.4 |  77.38 |        88.87 |        85.30 |  94.88 |          19.3 |
| 恒常フブキ1 / 水着ころね2 |      14.5 |      15.0 |   79.4 |  79.39 |        88.72 |        86.16 |  95.76 |          19.8 |
| 恒常フブキ1 / 恒常ころね3 |      15.1 |      15.5 |   77.4 |  77.37 |        90.75 |        85.84 |  92.86 |          19.3 |

候補族: スコア UP（条件解決済み / 条件無視の基準値）× 発動候補秒（基準周期 / 青の頻度込み）× 発動確率（p0 / 加算型青 / 乗算型青）× 効き方（up を 25% 増幅 / up に 25 を加算 / 誰かが発動中の秒に 25% が乗る）× 同時候補の扱い（production の `max(1, Σp)` / 割らない / 常に `Σp` / 候補人数）× 赤 X（入れない / up を `×(1 + X/100)`）× 候補人数の範囲（全部 / 1 人だけ / 2 人以上）。

**結果: 9 編成の絶対値を RMSE 0.8 以下で再現する候補はない。** 最良でも RMSE 1.00（候補人数で割る正規化 + 青タイムライン + 赤で up を増幅、ただし X 系列を 2.4 pt 外す）。実機のばらつき（13.4〜17.4、標準偏差 ≈ 1.3）に対して、候補の予測は 15 前後に集まり編成差を出せない。

観測から言える構造（式ではない）:

- **一様な倍率ではない。** `0.25 × E_base` / `0.25 × E_blue` / `0.25 × 線形和` はどれも +3.9 pt 以上の系統的過大で、上限 `0.25 × E_base` を 9 編成すべてが下回る。`C = k × Active` 型も否定（Active 77.7 → 72.1 と下がる入替で衣装は 14.1 → 17.4 と上がる）。
- **入替効果は加法ではなく相手枠に強く依存する。** 水着ころね2 → 恒常マリン1 の入替は、水着ミオ1 と組むと +3.3、水着フブキ0 と組むと +0.8、恒常フブキ1 と組むと +2.0。水着ミオ1 と恒常マリン1 は周期 23 秒・効果 8 秒が同一で候補窓が完全に重なり、その組が最大（17.4）。production の正規化（重なりを割り引く）とは逆向きの相互作用で、アクティブ評価器由来の量では説明できない。
- **条件つきスコア UP の扱い。** 条件解決済み up（水着ころね2 60 → 120、水着フワワ 50 → 100）で評価すると 10 組の 1 枚差の方向を 7 組以下しか当てないが、条件を無視した基準値で評価すると 9 組以上当てる。ただし絶対値は RMSE 1.4 で合わず、条件つきメンバーを衣装の対象外にする「効果 UP が重複しない」型も RMSE 1.6 以上。方向の観測として残す。
- **赤 X との交差は小さい。** 評価器の up を `×(1 + X/100)` すると +24 の衣装増分が 2 pt 以上過大になる（実機 −0.1〜+0.5、R-061 +3 で +0.1）。赤を入れなければ増分 0 で、実機の小さな増分（同一編成で 14.0 / 14.1 / 14.6）は未説明のまま。X=23 の値は水着フブキ0 13.6 / 水着ノエル0 13.3 / 恒常マリン1 15.5 とも報告されており（会話由来、X=23 は推定）、X 26 の値からいずれも −0.1。
- **量子化では救えない。** round / ceil / floor の差は RMSE で 0.1 以下。
- **水着フワワの青 45/0 と 0/0 の差**は、青を使う候補でも RMSE 差 0.5 以下で、どちらでも衣装欄を説明しない（provenance の裁定には使わない）。
- **negative control**: ピュア 1 人（恒常みこ入替）では衣装欄は非表示で、候補評価器も条件不成立で 0 を返す。
- 素値（P+T+S）で重み付けした評価器も試したが、9 編成の素値は 23.5k〜25.9k で差が小さく結果は変わらない（Lv 70 の 2 枚は最大 Lv の値で代用）。

公開情報: `by_live_leader_skill` / `LiveDeckEvaluation` / `liveDeckEvaluationCoefficientPermilMultiply` / `LiveDeckEvaluationScoreUpPermilUp` の算出コードや式は見つからなかった。攻略サイトの検索要約には「スコアサポートはスコア UP スキルの効果を上げる効果で、スコア自体を直接は上げない」「スコアボーナスはアクティブスキルと乗算で、アクティブが発動しているときだけ効く」という記述があった（外部解析・要約のみ。本文は本環境から取得できず未確認）。これは「アクティブのスコア UP を一様に ×1.25」の解釈と整合するが、その解釈は表示欄の絶対値では上の下限で反証されているので、表示評価器は実スコアの意味とは別の量を出していると考える。

**公開 proto（2026-09-13 にユーザーが外部調査で確認）。** `HolodoriDB/holodori-app-protos` の commit `8465f36b2ae64ac8982630b2550f2c0af55dbbfb` で、`jp/protobufs/rpc/api/common/live.gen.proto` に `LiveDeckInGameEffect.LiveActiveSkillLevel { live_active_skill_id, level }` と `live_leader_active_skill_levels` があり、`jp/protobufs/rpc/api/live.gen.proto` の `LiveGetDeckResponse` / `LiveSaveDeckResponse` は `LiveDeckEvaluation` と `LiveDeckInGameEffect` を同時に返す。**サーバー応答に解決済みの level が観測できる可能性がある**という証拠として記録する。フィールド名から「全メンバーのアクティブ level」とまでは断定しない（`live_leader_active_skill_levels` という名前が何を指すかは proto だけでは決まらない）。公開されている private server 実装 `yuvlian/symphytum-server` はこの配列を既定の空値にしていて、実サーバーの payload は公開 fixture に残っていない。応答そのものは取得していない。

この候補族の探索はここで打ち切る。その後の典獄クロニー 60% の観測（K5 / K6）で、衣装欄は青がないときはパッシブ支援の有無によらず `S/100 × アクティブ` に一致し、青があるときの配賦が問題だと分かった（上の「リーダー衣装のスコアサポート」）。次の観測もそちらの「次の実機観測（1 つ）」に統合する。

## 未解明

- **スコアサポート由来増分の Costume / Board / Passive へのカテゴリ別配賦**（総増分は赤・リーダー衣装とも `支援/100 × E_blue(乗算)`、支援 3 種は加算合成が強い推定。編成全体の逐次 marginal 6 順序、秒 × 候補単位の 5 ルール、Shapley はいずれも反証済み。衣装欄は青がなければパッシブ支援の有無によらず `S/100 × アクティブ` で、青があるときの配賦が未解明。次の本命）
- **`W_blue`（青ボード由来の重み）の決定式**（source 分離型 projective weight の唯一のボトルネック。`E_blue(加算) − H_C` は K1 / K2 を当てるが K3 / K4 を +3.5 過大にする。水着フブキ 0凸 を含む行に残差が集中）
- **カテゴリごとの raw weight `W` の式**（非 native 2 欄の比の保存は現コーパスで確定扱いだが、`C = λW_C / B = λW_B / P = λW_P` の `W` を決める式が未解明。既存の衣装候補族 436 個を `W_C` として入れ直しても 3 欄 RMSE 2.42 止まり。次の本命）
- **パッシブ欄の分割規則**（総量としては対象への静的な支援が強い推定だが、青があるとパッシブ欄はその 40〜61% になり残りがボード欄に出る。K6 では全量パッシブ欄。「青の純増分」「静的パッシブ」は和では λ = 1 と整合するが欄ごとの λ が 1.8〜2.9 倍ずれるので、そのままでは `W_B` / `W_P` にならない）
- 赤スコアサポートの一般式の確定（残差 ≤ 0.2 pt の由来、青の乗算型が一般仕様かどうか。K2 のボード欄はリーダー支援なしでも乗算型に一致）
- Costumeカテゴリの算出式（2026-09-12: 独立評価器候補族はすべて反証。クロニー 60% の K1〜K4 では最良候補 C* でも RMSE 1.3。上の「リーダー衣装のスコアサポート」）
- パッシブ欄の表示換算: 恒常マリン 1凸 9% の正しい入力で Golden 9.8〜9.11 の不足が 0.7〜1.8 に広がる
- Passiveスコアサポートの対象・合成位置
- 青の発動率UP / 発動頻度UPのBoard換算
- Costume / Board / Passive / SP の厳密な0.1%量子化規則
- SP残差

## 実験・実装時のルール

- Golden観測値をモデルに合わせない。
- 実機報告は最優先だが、単発報告を無誤謬と扱わない。`reported / rechecked / cross-checked` を分ける。
- 1ケースだけに合う定数を一般化しない。
- 同時に複数要因が変わった比較から単独因果を主張しない。
- カード事実はID・開花・role・provenanceを確認してから使う。
- モデル変更前に既存コーパスで反証を探す。
- 実ライブのスコア式と表示ユニットスコアを混同しない。
