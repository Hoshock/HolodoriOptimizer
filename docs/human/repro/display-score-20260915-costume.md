# 2026-09-15: 衣装欄の量子化の位相（クロニー 60% × 青なし 8 編成）

リーダーを 典獄クロニー（全員のスコアサポート 60%。衣装スキルは開花で変わらない）にし、**青 0・赤 0・黄 0・曲なし**の 5 人編成で **アクティブ欄と衣装欄**を読んだ。`reported`（単発）。

この条件では production の衣装欄 raw は `0.60 × アクティブ欄 raw` ちょうどになるので、残る自由度は**表示 0.1 刻みへ落とす位相だけ**になる。

## 観測

**アクティブ欄は 8 編成すべて実機と一致した**（ユーザー確認）。したがって衣装欄の食い違いは入力ではなく位相の問題。

|   # | メンバー 5 人                                     | アクティブ欄 | 実機 衣装欄 | 独立に切り上げ | アクティブ欄との差 | モデルのパッシブ欄 raw |
| --: | :------------------------------------------------ | -----------: | ----------: | -------------: | -----------------: | ---------------------: |
|   1 | マリン1 / ロボ子0 / スバル0 / アーニャ0 / レイネ0 |         69.3 |        41.5 |         41.6 ✗ |             41.5 ✓ |                   2.39 |
|   2 | ロボ子0 / ポルカ0 / リス0 / ビジュー0 / ネリッサ1 |         67.3 |    **40.6** |         40.4 ✗ |             40.3 ✗ |                   8.76 |
|   3 | ぺこら1 / フレア0 / スバル0 / IRyS0 / ビジュー0   |         65.8 |        39.5 |         39.5 ✓ |             39.4 ✗ |                   2.02 |
|   6 | ぺこら1 / フレア0 / アーニャ0 / こぼ0 / 奏0       |         68.3 |        40.9 |         41.0 ✗ |             40.9 ✓ |                      0 |
|   7 | ぺこら1 / イオフィ0 / レイネ0 / IRyS0 / 奏0       |         65.3 |        39.1 |         39.2 ✗ |             39.1 ✓ |                      0 |
|   8 | スバル0 / アキ0 / アーニャ0 / こぼ0 / IRyS0       |         56.6 |        33.9 |         34.0 ✗ |             33.9 ✓ |                      0 |
|  K5 | そら0 / アキ0 / スバル0 / フレア0 / ぼたん0       |         63.3 |        37.9 |         38.0 ✗ |             37.9 ✓ |                      0 |
|  K6 | そら0 / アキ0 / スバル0 / フレア0 / マリン1       |         73.7 |        44.3 |         44.3 ✓ |             44.3 ✓ |                   2.85 |

K5 / K6 は 2026-09-12 / 09-13 の観測を 2026-09-15 に再読して値が変わらないことを確認したもの。

## 編成 1 / 2 / 3 の 5 欄（2026-09-15 追加）

|        # |     衣装 | アクティブ | ボード | パッシブ |   SP |  合計 |
| -------: | -------: | ---------: | -----: | -------: | ---: | ----: |
|   1 実機 | **41.5** |       69.3 |      0 |  **2.5** | 40.3 | 153.6 |
| 1 モデル |     41.6 |       69.3 |      0 |      2.4 | 40.3 | 153.6 |
|   2 実機 | **40.6** |       67.3 |      0 |  **8.6** | 39.9 | 156.4 |
| 2 モデル |     40.4 |       67.3 |      0 |      8.8 | 39.9 | 156.4 |
|   3 実機 |     39.5 |       65.8 |      0 |      2.1 | 38.9 | 146.3 |
| 3 モデル |     39.5 |       65.8 |      0 |      2.1 | 38.9 | 146.3 |

**合計・アクティブ欄・ボード欄・SP 欄は 3 件とも完全一致**し、食い違うのは**衣装欄とパッシブ欄のあいだで 0.1〜0.2 が入れ替わっているぶんだけ**。総量 `T` は合っていて、配分か量子化の位相だけがずれている。

### 編成 2 は再読でも 40.6（`衣装欄 = 0.60 × アクティブ欄` の棄却）

再読で 総合力の内訳と ユニットスコアも取った。

```txt
ユニットスコア 555096
総合力 106264（メンバーパラ 75066 / ホロメンボード 20265 / パッシブ 3380 / メモリー 4510 / 強化ボーナス 3043）
スコアボーナス 156.4（衣装 40.6 / アクティブ 67.3 / ボード 0 / パッシブ 8.6 / SP 39.9）
```

`ceil(106264 × (1 + 156.4/100) × 2.03734) = 555096` で**外側の式が 1 点単位で一致**するので、合計 156.4 は `cross-checked`。

アクティブ欄 67.3 から `0.60 × A ≤ 0.60 × 67.3 = 40.38`。**切り上げ・四捨五入・切り捨てのどれで落としても 40.4 以下**にしかならず、実機 40.6 と両立しない。3 欄の配分は projective で `W_P = ΔP` が約分されるため、パッシブの対象をどう選んでも衣装欄は `0.60 × A` のまま動かない。**「衣装欄 raw = 0.60 × アクティブ欄 raw」はこの 1 件で棄却される。**

### カードの Lv はスキルの数値を変えない

メンバーパラメータ 75066 は 5 人で平均 15,013 で、`cards.json`（上限側）の素値合計 約 23,600 の **64% 程度**しかない。**それでもアクティブ欄は 8 編成すべて実機と一致した**（SP 欄も 3 件一致）。したがって**スキルの数値（スコア UP % ・効果時間・発動周期・スコアサポート %）は Lv に依存しない**と扱ってよい。Lv が効くのは パラメータと、それを介した「N人」の対象選抜だけ。

### パッシブの対象選抜では説明できない（2026-09-15 に取り下げ）

編成 1 の ハッピータイプ 3 人のカード詳細 P/T/S は マリン 8672 / 11775 / 9498（合計 29945）、レイネ 5197 / 6386 / 5619（17202）、ロボ子 7006 / 5030 / 5081（17117）。**順位は マリン > レイネ > ロボ子 で、モデルが選ぶ マリン + レイネ と同じ**。緑ボードの解放も レイネ と ロボ子 は同じ 4 マスなので素値でも順位は変わらない。

したがって「実機は マリン + ロボ子 を選んでいる（対象を強制すると パッシブ raw 2.4374 で要求区間に入る）」という読みは**成り立たない**。編成 1 の 0.1 も、配分か量子化の位相の側にある。

## 読み取れること

1. **パッシブ支援がない 4 件（K5 / 6 / 7 / 8）は全会一致**。独立に切り上げる現行の規則は 4/4 外し（すべて実機より 0.1 高い）、「アクティブ欄との差として量子化する」形が 4/4 当てる。**現行の位相は棄却**。
2. **1 と 3 は判定に使えない。** 判定は `ceil(1.6 × A)` がどちら側に落ちるかで決まるが、この 2 件は `1.6A` が 0.1 の境界から 0.0005 / 0.0008 しか離れていない（編成を選ぶときに `A` と `0.6A` の境界距離しか見ていなかった設計ミス）。raw が 0.001 動けば答えが反転するので、1 と 3 が逆を向いたことは矛盾ではない。
3. **残る食い違いはパッシブ支援がある編成に限られ、衣装欄とパッシブ欄のあいだの移動になっている。** 総量 `T`・アクティブ欄・ボード欄・SP 欄・合計はすべて合う。必要な raw の移動は 編成 1 が `(−0.05, +0.05)`、編成 2 が `(+0.16, −0.16)`、編成 3 が 0。**符号が逆なので、`W_P` を定数倍する形（`W_P = k × ΔP`）では両立しない** — 編成 1 は `k ≥ 1.022`、編成 2 は `k ≤ 0.979` を要求する。
4. **次に要るのは 編成 1 と 2 を、リーダーだけ 恒常みこ 0凸（スコアサポート 0）に替えた baseline。** 支援 0 ならパッシブ欄がそのまま `ΔP` になるので、配分を経由せずに `T` の内訳が読める。モデルの予測は 編成 1 が パッシブ 2.4、編成 2 が パッシブ 8.8。ここが実機とずれていればパッシブ支援の評価そのもの、合っていれば配分だけの問題に絞れる。

## 支援なしリーダーの baseline（2026-09-15）

同じメンバーで**リーダーだけ 恒常みこ 0凸（スコアサポート 0）**に替えた。支援 0 ならパッシブ欄がそのまま `ΔP` になる。

|   # | リーダー     | 衣装 | アクティブ | ボード | パッシブ |   SP |
| --: | :----------- | ---: | ---------: | -----: | -------: | ---: |
|   1 | 恒常みこ0    |    0 |       69.3 |      0 |  **2.4** | 40.3 |
|   1 | 典獄クロニー | 41.5 |       69.3 |      0 |  **2.5** | 40.3 |
|   2 | 恒常みこ0    |    0 |       67.3 |      0 |  **8.8** | 39.9 |
|   2 | 典獄クロニー | 40.6 |       67.3 |      0 |  **8.6** | 39.9 |

**baseline は 2 件ともモデルと完全一致**（予測 2.4 / 8.8）。つまり `ΔP`（パッシブのスコアサポートがタイムラインへ足す量）も、総量 `T = 0.60 × A + ΔP` も正しい。

決定的なのは、**リーダー支援を足すとパッシブ欄が動く**こと。編成 1 は `2.4 → 2.5`（+0.1）、編成 2 は `8.8 → 8.6`（−0.2）。**production は `W_P = ΔP` なので配分が約分され、パッシブ欄は動かない**（K5 / K6 でも実機が動かなかったので気づけなかった）。3 欄の再配賦そのものは実機で起きていて、その重みがモデルと違う。

必要な補正は `W_P` を 編成 1 で 2〜6% 大きく、編成 2 で 2〜3.5% 小さく。**符号が逆なので定数倍では両立しない。** 編成 2 だけ、同じ 2 人に 11% が 3 枚積まれて +33% になっている（他はすべて +11%）。**同じメンバーへ積み重なった支援は、重み `W_P` へは線形に効かないのではないか** — これが現時点の作業仮説。

## 積み重ね系列 S1 / S2 / S3（2026-09-15）

青 0・赤 0・黄 0・曲なしで、**同じメンバーへ積み重なる支援の量だけ**を変えた。リーダーは 恒常みこ 0凸（支援 0）と 典獄クロニー（60%）の 2 通り。

| 系列 | メンバー 5 人                                   | 積み重ね   |  `ΔP` | みこ | クロニー 実機                    | クロニー モデル |
| :--- | :---------------------------------------------- | :--------- | ----: | :--- | :------------------------------- | :-------------- |
| S1   | ロボ子0 / レイネ0 / スバル0 / アーニャ0 / 奏0   | 2 人に 11% | 2.205 | 一致 | 衣装 39.8 / パッシブ 2.3         | 39.8 / 2.3 ✓    |
| S2   | ロボ子0 / ポルカ0 / スバル0 / アーニャ0 / 奏0   | 2 人に 22% | 4.360 | 一致 | 衣装 **39.4** / パッシブ **4.3** | 39.3 / 4.4      |
| S3   | ロボ子0 / ポルカ0 / リス0 / スバル0 / アーニャ0 | 2 人に 33% | 6.166 | 一致 | 衣装 **35.4** / パッシブ **5.7** | 34.8 / 6.2      |

**支援 0 のリーダー側は 3 系列とも完全一致**。つまりアクティブ欄・`ΔP`・SP 欄はすべて正しく、ずれるのは**リーダー支援を足したときの 3 欄の配分だけ**。

実機の衣装欄から `W_P` の必要倍率を逆算すると、**同じメンバーへ積み重なった支援の量に対して単調に下がる**。

| 材料             | 同じメンバーへの支援     | `W_P` の必要倍率       |
| :--------------- | :----------------------- | :--------------------- |
| S1 / 編成 3 / K6 | 11〜12%                  | 0.96〜1.01（補正なし） |
| S2               | 22%                      | 0.951〜0.976           |
| 編成 2           | 33%（+ 別の 2 人に 11%） | 0.965〜0.979           |
| S3               | 33%                      | **0.885〜0.903**       |

（編成 1 だけ 1.022〜1.067 と逆を向くが、これは `0.60 × A` が 0.1 の境界を 0.05 だけ超えている 1 quantum のケースで、**衣装欄を「アクティブ欄との差」として量子化すれば補正なしで 41.5 が出る**。位相の問題として切り離せる。）

**総量 `T = 0.60 × A + ΔP` は全件で正しい。** 動いているのは配分の重み `W_P` だけで、**同じメンバーに積み重なった支援は重みへ線形に効かず逓減する**、というのが現時点の読み。

## S4 と全観測の突き合わせ

S4（ロボ子0 / レイネ0 / ビジュー0 / ネリッサ1 / スバル0。**4 人がそれぞれ 11%、積み重ねなし**、`ΔP` 4.395）は みこ側が一致し、クロニー側は **衣装 40.5 / パッシブ 4.4**（モデルは 40.4 / 4.4）。

**対象選抜は全件、支援 0 側のパッシブ欄で一意に決まった**（S3 は ロボ子+ポルカ でしか 6.2 にならず、編成 2 は 8.8、編成 1 は 2.4 で同様）。したがって `ΔP` は確定値で、逆算した必要倍率も確定する。

| 材料   | 同じメンバーへの支援     |  `ΔP` | `ΔP / A` | `W_P` の必要倍率  |
| :----- | :----------------------- | ----: | -------: | :---------------- |
| 編成 3 | 11%（2 人）              | 2.025 |    0.031 | ≈ 1（モデル一致） |
| S1     | 11%（2 人）              | 2.205 |    0.033 | 0.998〜1.007      |
| K6     | 12%（2 人）              | 2.853 |    0.039 | ≈ 1（モデル一致） |
| S2     | 22%（2 人）              | 4.360 |    0.067 | 0.959〜0.976      |
| S4     | 11%（**4 人**）          | 4.395 |    0.065 | 0.966〜0.991      |
| S3     | 33%（2 人）              | 6.166 |    0.106 | 0.894〜0.903      |
| 編成 2 | 33%（2 人）+ 11%（2 人） | 8.762 |    0.130 | 0.965〜0.979      |

**S2 と S4 の区間は重なる**（0.966〜0.976）ので、「積み重ね」と「総量」を分けるつもりだった S4 は**決め手にならなかった**。そして **編成 2 が両方の読みを壊す**: `ΔP` も `ΔP / A` も最大なのに必要倍率は 0.97 で、`ΔP` が小さい S3 の 0.89 より 1 に近い。

- **総量（`ΔP` や `ΔP / A`）の単調関数ではない**（編成 2 < S3 の向きが逆）。
- **メンバーごとの支援 % の関数 `f(s)` を重みに使う形でも合わない**。`f(0.33)/0.33 ≈ 0.89`・`f(0.11)/0.11 ≈ 1` を仮定すると、編成 2 の 0.97 を出すには 33% ぶんが `W_P` の 27% しか占めないことになり、実際の内訳（33% が 2 人、11% が 2 人）と矛盾する。

**確定しているのはここまで**: 総量 `T = 0.60 × A + ΔP` は全件で正しい。アクティブ欄・SP 欄・`ΔP`・合計・ユニットスコアも全件正しい。**残るのは 3 欄の配分の重み 1 箇所だけ**で、その形がまだ出ていない。次は観測を増やす前に、自由係数を持たない候補の族を機械比較する（コーパス 153 列 + この 9 件）。

## 位相候補の機械比較

| 位相                                 | コーパス 153 列 | この 8 件の衣装欄 |
| :----------------------------------- | --------------: | ----------------: |
| 欄ごとに独立に切り上げ（現行）       |              59 |         **2 / 8** |
| 累積（パッシブ → 衣装 → ボード）     |          **61** |             6 / 8 |
| 各欄をアクティブ欄との差として量子化 |              60 |             6 / 8 |
| 累積（衣装 → ボード → パッシブ）     |              53 |             6 / 8 |

どの候補も 2 を外す。**位相を決める前にパッシブ支援の扱いを片付ける必要がある** — 1・2・3 の 5 欄すべて（とくにパッシブ欄）が要る。パッシブ欄も一緒にずれていれば総量 `T` の問題、衣装欄だけなら 3 欄の配分の問題。

固定は `src/engine/displayScoreCostumeQuantization.test.ts`。
