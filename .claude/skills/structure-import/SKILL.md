---
name: structure-import
description: |
  What: 各ユーザーが自分のゲーム内スクリーンショットをローカルの LLM に読ませ、その出力をホロドリ編成お助けツールへ貼り付けて取り込むための構造化データ（インポート用 JSON）の共通形式と作り方を定義する。第一弾は所持メンバー（★5 カードと開花段階）。
  Use when: スクショからアカウント情報（所持メンバー・ボード・メモリー・強化ボーナスなど）を取り込む話が出たとき、インポート用 JSON を実際に作るとき、その形式を設計・変更するとき、サイト側の取り込み UI を実装するとき、ローカルのエージェントに渡す指示を書くとき。
version: 2026-09-10-05
---

# スクショからの構造化インポート (structure-import)

このスキルは、ホロドリ編成お助けツールが**貼り付けて取り込める JSON の形**を定義する。想定する流れは「ユーザーが自分のゲーム内スクショを**自分の端末のローカル LLM** に渡す → このスキルの指示に従って構造化データを作らせる → その JSON をコピーしてサイトの取り込み欄に貼る」。画像はどこにもアップロードされず、サイトはサーバを持たないので、貼った内容はそのブラウザの `localStorage` に入るだけである（`.claude/rules/storage-compat.md`）。種別ごとのスキーマは References にあり、このファイルは全種別に共通する封筒・禁止事項・手順だけを持つ。

**このファイルはサイトの取り込み画面のプロンプトから URL で直接読まれる**（`src/ui/importPrompt.ts` が渡すのは「次のスキルを実行してください」+ raw の URL だけで、指示はすべてここにある）。読み手はこのリポジトリを持っていない前提で書く: 相対リンクは辿れないので References には raw の URL を併記する。

## URL で渡されたときの実行手順

「次のスキルを実行してください」とこのファイルの URL だけを渡された場合は、次のとおり動く。

1. **添付のスクリーンショットを読む。** 添付が無ければ、何をどう撮ってほしいかを伝えて止まる（種別ごとの「必要なスクショ」の節）。
2. **種別の既定は所持メンバー**（`kind: "owned-members"`）。ユーザーが別の種別を指定したらそれに従う。**スキーマは References のファイルにしかないので、必ず読んでから書き始める**（raw の URL を取得する）。
3. 下の「守ること」に従って読み取り、「共通の封筒」に入れた JSON を作る。**並びは画面に写っている順**（手順 3 の読み取り順）。
4. **出力はコードブロック 1 つの JSON だけ**にする（前置き・解説を書かない）。ユーザーはそれをコピーしてサイトの取り込み欄に貼る。読み取れなかったものは `unreadable` に入れ、コードブロックの外に短く伝えてよい。
5. このファイルや References の URL を取得できないときは、**形式を推測せず「読めなかった」と伝える**（捏造した JSON は取り込み側で弾けない）。
6. リポジトリのファイルを書き換える必要はない。作るのは JSON だけ。

## External References

| ファイル                                                                                                                                                                                            | 説明                                                                                    | いつ読むか                                                                                               |
| :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------- |
| [references/owned-members.md](./references/owned-members.md)（raw: `https://raw.githubusercontent.com/Hoshock/HolodoriOptimizer/main/.claude/skills/structure-import/references/owned-members.md`） | 所持メンバー（`kind: "owned-members"`）のスキーマ・記入例・照合の指針・取り込み側の扱い | 所持メンバーの JSON を作るとき（**スクショから作る依頼なら必ず読む**）、その取り込みを実装・変更するとき |

## 共通の封筒

種別が増えても貼り付け口を 1 つに保つため、どの種別も同じ封筒に入れる。

```json
{
  "format": "holodori-optimizer/import",
  "version": 1,
  "kind": "owned-members",
  "capturedAt": "2026-09-10",
  "notes": ["人間向けの補足。任意"],
  "unreadable": [{ "reason": "指で隠れている", "hint": "3 枚目の右下" }]
}
```

- `format` / `version` / `kind` は必須。`format` は固定文字列で、無関係な JSON を貼ったときにサイト側が弾くための目印。`version` は封筒の版で、フィールドを増やしても読めるように上げない（互換を壊す変更のときだけ上げる）。
- 種別ごとの本体（所持メンバーなら `cards`）は `kind` に対応するキーへ入れる。詳細は References。
- `capturedAt`（スクショを撮った日付）・`notes`・`unreadable` は任意。`unreadable` は**読み取れなかったものを黙って落とさないため**の枠で、人間が後から手で埋める材料になる。
- 出力は**コードブロック 1 つの JSON だけ**にする（ユーザーはそれをそのままコピーする）。説明はコードブロックの外に書く。

## 守ること

- **推測で埋めない。** 読めなかったものは `unreadable` に理由つきで置く。0凸と読み間違えるより、未読取のまま人間に回すほうが安い（試算がずれると原因が追えなくなる）。
- **スクショ・画像そのものを出力やリポジトリに持ち込まない。** base64 の埋め込みも不可。公式アセットをコミットしない方針（ADR-002）は、この経路にも同じく効く。
- **プレイヤー名・ユーザー ID・フレンドコード・課金履歴などの個人を特定する情報を出力に含めない。** スクショの隅に写っていても構造化データには入れない。
- **ゲームクライアントの解析・データダンプ由来のマスターデータを使わない**（ADR-002）。名前の確認はスクショの目視とネット検索（攻略サイトの一覧）で行い、**両者が食い違ったらスクショ（実機）を優先する**。
- **カードの数値（P/T/S・レベル）は出力しない。** ツールはカードデータ側の値を使い、ユーザーの入力として保存するのは「どのカードを持っているか」と育成状態だけ（`.claude/rules/storage-compat.md`）。

## 手順

1. どの種別を作るのか決め、対応する References を読む（所持メンバー = `owned-members`。スキーマはそこにしかないので、読まずに書き始めない）。
2. 必要なスクショが揃っているか確かめる（種別ごとの「必要なスクショ」の節）。足りなければ、何をどう撮ってほしいかを具体的に伝える — 撮り直しのほうが推測より速い。
3. スクショ 1 枚ずつ、写っているものを列挙する。**読み取る順は左上から右下** — 1 枚の中では左上から右へ進み、行が終わったら次の行の左端へ折り返す。複数枚は渡された順に続ける。複数枚にまたがる重なりは最後に 1 件へまとめる（残すのは先に出てきたほう）。
   **配列の並びはこの読み取り順のまま**にし、名前順・レア度順などに並べ替えない — 人が画面と JSON を上から見比べて検算できるようにするため（2026-09-10 ユーザー指示）。
4. 読み取りが怪しい固有名詞（漢字・カタカナ・英数の混在）はネット検索で正式表記を確認する。確認できなければ `unreadable` へ。
5. 封筒に入れた JSON をコードブロック 1 つで出す。
6. コードブロックの外に、人間が確認すべきことだけを短く添える: 件数、`unreadable` の中身、判断に迷った行。

## 出力前の自己チェック

- 種別ごとの必須フィールドが全件そろっているか。型・範囲（整数か、上限下限）を外していないか。
- 同じ対象を 2 行に分けていないか（スクショの重なり）。
- 配列の並びが画面の順（左上 → 右下、スクショは渡された順）になっているか。
- 推測で埋めた値が残っていないか。
- 個人を特定する情報・画像・カードの数値が混ざっていないか。
- JSON が単体で妥当か（末尾カンマ・全角の引用符・コメントが入っていないか）。
